# ── תלויות ──
node_modules/

# ── תוצרי בנייה (‏1GB — נבנים מחדש בכל build) ──
desktop/dist/
# renderer/index.html מסונכרן אוטומטית מ-MyWord-v2.html — אל תעריך ידנית
desktop/renderer/index.html

# ── תוצרי בדיקות (‏875MB צילומי מסך) ──
qa/screenshots/
qa/reports/
# סקריפטי בדיקה זמניים (nameing convention: _*.mjs)
qa/_*.mjs
qa/*.log

# ── גיבויים מקומיים (96MB — לא נחוצים בהיסטוריית git) ──
backups/
backup/
*.backup-*.html
MyWord-v2.html.orig_test

# ── סודות ──
.env
qa/.env
qa/.env.summer

# ── מערכת ──
Thumbs.db
desktop.ini
.DS_Store
