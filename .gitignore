# ── תלויות ──
node_modules/

# ── תוצרי בנייה (‏1GB — נבנים מחדש בכל build) ──
desktop/dist/
desktop/renderer/index.html      # מסונכרן אוטומטית מ-MyWord-v2.html

# ── תוצרי בדיקות (‏875MB צילומי מסך) ──
qa/screenshots/
qa/reports/
qa/_*.mjs                        # סקריפטי בדיקה זמניים
qa/*.log

# ── גיבויים מקומיים (96MB — לא נחוצים בהיסטוריית git) ──
backups/
backup/
*.backup-*.html
MyWord-v2.html.orig_test

# ── סודות ──
.env
qa/.env
qa/.env.summer

# ── מערכת ──
Thumbs.db
desktop.ini
.DS_Store
