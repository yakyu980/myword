# CLAUDE.md — MyWord v2 · חוקת הפרויקט
> קובץ זה מגדיר את **חוקי הבסיס** של MyWord.
> כל פיצ'ר חדש חייב לעבוד לפיהם. **אסור לשנות חוקים אלה בלי אישור מפורש.**
>
> _סונכרן מול הקוד: 2026-07-27 (**סאמר — סגירת פערי `#insShapeCaret`/`#insImageCaret`/`#insTextboxCaret`/`#insTableCaret` מול ctxBar/לשוניות-הקשר, לפי מחקר-spec של נוי (2026-07-27)**. בקשת-משתמש: "מה שיש בסרגל הצף לצורות/תמונות/טבלאות שיהיה גם בסרגל הראשי, תחת החץ של אפשרויות נוספות". נוי תיעדה מראש ב-3 קבצי-SPEC (`SPEC-INSERT-IMAGES.md`/`SPEC-INSERT-TABLE.md`/`SPEC-INSERT-TEXTBOX.md`) את הפער המדויק (13-14 יכולות חסרות בתמונה/צורה, 8 בתיבת-טקסט, 6 בטבלה) + המלצה מפורשת "5-6 הכי-שימושיים לכל כפתור, לא הכל בבת-אחת — אחרת ה-▾ הופך לרשימה בלתי-שמישה". **עקרון-עבודה בכל 4 השלבים: אדיטיבי בלבד — כל `itm(...)` חדש קורא לאותה פונקציה-פאנל בדיוק שכבר קיימת ומוכחת ב-`imgformat`/`shapeformat`/`tableformat`/ctxBar, לא נבנה מנגנון חדש** (אותה תבנית שכבר הוכחה 2026-07-26 לטבלה). **(1) `#insShapeCaret`** (MyWord-v2.html~6772, הפער הגדול ביותר) — hint חדש למצב "בלי בחירה" (היה קופץ ישר לבורר בלי לרמוז שיש תפריט-כלים עשיר); עם בחירה נוספו 🌤️ צל (`openShapeShadowPanel`) · 🎨 גרדיאנט (`openShapeGradientPanel`) · 📐 מיקום מדויק (`openShapeTransformPanel`) · 🔺/🔻 ריבוד-צעד-אחד (`_stepZOrder`, לא היה כלל) · 🔒/🔓 נעל-שחרר. **(2) `#insImageCaret`** (~20984) — ✂️ הסר-רקע (`openBgRemovePanel`) · 🌗 מסננים (`openImageFiltersPanel`) · 🎴 סגנונות-מוכנים (`openImageStylesPanel`) · 📐 מיקום-מדויק · 🔒/🔓 נעל-שחרר · ♻️ שחזר-מקורי (בלוק-שחזור-מלא, כולל גיאומטריה דרך `_imgRestorePreCrop`, זהה ל-imgformat). **(3) `#insTextboxCaret`** (~21094) — 🖼️ מסגרת-מתקדמת (`openTextboxBorderPanel`) · ↕️ יישור-טקסט-אנכי (מעבר-מחזורי, `window._setTbValign`) · 📐 מיקום-מדויק · 🌤️ צל (`openTextboxShadowPanel`) · 🔗 קשר-לתיבה-הבאה/זרימת-טקסט (`_tbStartLinkPick`). **(4) `#insTableCaret`** (~15111, עדיפות-נמוכה — `tableformat` כבר חושף הכל מיד עם כניסת-סמן לתא) — 📏 AutoFit (`openTableAutofitPanel`) · 🎴 סגנון-טבלה (`openTableStylePanel`) · ↕️ מיין-טבלה (`openTableSortPanel`). **אימות:** syntax 0 שגיאות אחרי כל שלב; 4 סקריפטי-Playwright ייעודיים (הכנסת-אובייקט-בדיקה → בחירה → פתיחת-▾ → קליק על **כל** פריט-חדש → אימות שהפאנל-הנכון נפתח) — כל 24 הפריטים החדשים פועלים, 0 pageerror/console.error. מרקוס `insert` **75/75** (אחרי כל אחד מ-4 השלבים) + `free-objects` **19/19** + `page-rules` **18/18** — כולם ללא רגרסיה. **לא בוצע (מוצהר):** שאר היכולות (intersect/exclude מפורט, alt-text+דחיסה, Autofit/Format-Painter/clip-path לתיבה) נשארות נגישות רק דרך `shapeformat`/`imgformat`/ctxBar-"עוד" הקיימים — לפי עיקרון "לא להציף" מה-spec._
>
> _קודם — 2026-07-26ג (**סאמר — טקסט-רגיל נעלם לצמיתות בפער-בין-עמודים: `_splitTextBlock` אכל רווח-גבול-חיתוך**. דיווח-משתמש: "טקסט עובר בין הדפים ולא שומר על גבולות הגיזרה שלו, וכשאני מוריד אותו למטה הוא נשאר בין שני דפים איפה שלא רואים" — אושר שזה טקסט-רגיל (פסקה), לא אובייקט-חופשי. **שחזור-חי (לא syntax-בלבד):** הקלדה אמיתית תו-אחר-תו (Playwright, לא `execCommand`) לתוך פסקה יחידה עד שגובהה חוצה כמה עמודים. **שורש:** מנגנון קיים לפיצול-פסקה-גבוהה-מעמוד בגבול-מילה (`_paginateTallBlocks`/`_splitTextBlock`, MyWord-v2.html~16606/16547) הכיל `.replace(/\s+$/,'')` (16591, כפי-שהיה) שהסיר את רווח-הגבול מ"ראש" הפסקה בכל חיתוך, בעוד ה"זנב" (`chunk`) מתחיל תמיד **אחרי** אותו רווח — הרווח לא נשמר באף צד. `_mergeListChunks` (רץ בתחילת כל `updatePagination()` כדי לאחות פיצולים-קודמים) מאחה בפשטות (`appendChild`+`normalize`, בלי הוספת-רווח) — כך שבכל סבב הקלדה שמאחה-ואז-מפצל-מחדש נעלם עוד רווח-גבול לצמיתות, עד שקטע-טקסט שלם מאבד **כל** הרווחים שלו ו-`_splitTextBlock`'s `cuts.length<1 → return false` נכנס לפעולה לצמיתות — הפסקה נשארת בלוק אטומי-ענק, נדחפת כשלם ע"י `paginateBlocks` (עמוד אחד בלבד) ולא מתפצלת שוב אף פעם, כך שחלק ממנה נופל בפסי-הפער הבאים ונחתך ע"י ה-CSS mask. **אימות-שורש:** פסקה של 11,589 תווים הקלדה-אמיתית → 2 אלמנטים בלבד, האחרון בגובה 5348px ללא פיצול נוסף, `totalLen` בפועל 10,690 (899 תווי-רווח אבדו), `cutCount:0`. **תוקן (MyWord-v2.html:16590-16597, נקודתי):** הוסרה `.replace(/\s+$/,'')` — `cut.off` כבר מיד-אחרי ריצת-הרווח, כך שה"ראש" כבר כולל אותו בסופו; אין צורך "לנקות". **אימות אחרי-תיקון (אותו תרחיש):** 6 אלמנטים ממוקמים בדיוק בתחילת-כל-עמוד, 0 straddlers, `totalLen` תואם בדיוק לאורך-ההקלדה (11,589=11,589) בשני מבחני-חזרה (עברית+מספרים, ASCII נקי). **ממצא-נלווה לא-בהיקף (מדווח, לא תוקן):** סדר-תווים בתוך טקסט-מאוחה יכול להיראות מעורבב בהמשך-הקלדה אחרי אירוע-פיצול (חשד לקפיצת-Selection אחרי מוטציית-nodeValue ישירה ב-`_splitTextBlock`) — לא גורם ל"נעלם בפער" (0 straddlers, אורך תקין), סימפטום נפרד מהבאג המדווח; דורש חקירה נפרדת אם המשתמש ירצה. מרקוס `page-rules` **18/18** · `insert` **75/75** · `home-ribbon` **46/46**, 0 שגיאות console. פירוט: wiki/log.md "2026-07-26"). קודם — 2026-07-26 (**צורות — לולאת qa-flow: 6 ליקויים + 2 בקשות, כולם נסגרו (אישור-משתמש: "הכל תוקן")**. **★★ שורש-על שהסביר 3 תלונות בבת-אחת:** ה-media query `@media (max-width: 768px), (pointer: coarse)` (2 מופעים, ~1400 ו-~2018) הסתיר את `#ctxBar` ב-`!important` והציג את `#mobBar` במקומו. התנאי `(pointer: coarse)` **לבדו** מופעל על כל מכשיר עם יכולת-מגע — כולל מחשב עם עכבר וחלון רחב. זה הסביר לבדו את "אין סרגל צף לצורות" + "מופיע סרגל עם צל על כל הדף" (=`#mobBar`: fixed, bottom-center, עד 600px, זכוכית-חלבית+צל) + "הכל זז ולא נוח לעבוד" (מודל-אינטראקציה שונה של הסרגל הנייד). **תוקן** ל-`(pointer: coarse) and (hover: none)` — התקן ל"מכשיר-מגע אמיתי"; מחשב עם עכבר תמיד `hover: hover` ולכן שומר על הסרגל הרגיל. ⚠️ **לקח-אבחון:** בדיקה סינתטית הראתה ש-`#ctxBar` תקין לגמרי (display:flex, 14 כפתורים, on-screen 352×109) בזמן שהמשתמש דיווח "אין סרגל" — כשזה קורה הפער בסביבה ולא בלוגיקה; `AskUserQuestion` עם תיאור ויזואלי קונקרטי פתר בשאלה אחת. **★ מיזוג היה בלתי-הפיך (אובדן-תוכן):** `combineSelectedShapes`/`paperBooleanOp` מוחקים את צורות-הקלט, אך אף אחד מ-3 מסלולי-הקריאה (`doCombine` · `_ctxDoCombine` · `mergeOp` בריבון) לא קרא ל-`_historyRecord`, וה-MutationObserver של ההיסטוריה מתעלם במכוון מ-`.free-obj` → Ctrl+Z לא החזיר כלום. תוקן בנקודה משותפת אחת (ראש `combineSelectedShapes`, אחרי בדיקת `<2`); אומת: 2 צורות → חיתוך → Ctrl+Z → שתיהן חזרו. **★ חיתוך השאיר תיבה בגודל האיחוד:** `paperBooleanOp` חישב `const rbounds = result.bounds` ו**לא השתמש בו כלל** (משתנה-מת), בעוד W/H נלקחו מ-`maxR-minL`/`maxB-minT` (bbox של שתי צורות-הקלט) → אחרי subtract הידיות חבקו אוויר. תוקן: חיתוך ל-`result.strokeBounds` (נפילה ל-`bounds`; `bounds` לבדו חותך את עובי-הקו) + הזזת `left`/`top` ב-`cropX`/`cropY` לשמירת המיקום. אומת: 400px(איחוד)→245px(תוצאה) — אותו דפוס כמו crop-to-object בהסרת-רקע. **גם:** (א) **מרווח מקצה-הדף** — ה-gap-avoidance ב-`_clamp` דחף בדיוק ל-`gapStart`/`gapEnd` (מרווח **0**) במקום ל-`topMargin`/`botMargin`; תוקן ל-`upTop`/`downTop` (אותה נוסחה כמו `docTop`/`docBot`, יחסית ל-`cycTop` של הפער), אומת 38px משני הכיוונים ומעבר-חופשי לא נפגע. (ב) **לשונית-צורה נפרדת בסרגל הצף** ("לצורות טאב משלו ולא מתחת לתמונה") — `TAB_ORDER` → `[...,'image','shape']`, ענף `'shape'` ב-`switchTab` (שימוש-חוזר ב-`buildImage(obj,false)` הקיים — רק הניתוב הופרד; "תמונה" מטפלת מעכשיו בתמונה אמיתית בלבד), ומיפוי `ctx='object'`→`'shape'` בגלגלת. (ג) **בחירת צורה מתחת לאחרת** (Alt+לחיצה) — נבדק חי ונמצא **תקין**, לא שונה. (ד) **רקע-שקוף לצורות** — נבדק ונמצא **כבר קיים ועובד** (`fill:'none'` דרך `showNone`; מיכל+wrap כבר `rgba(0,0,0,0)`); סווג UX-Clarity/גילוי ולא נכתב קוד. מרקוס: `insert` **75/75** · `free-objects` **19/19** · `page-rules` **18/18** · `ctx-tabs` 6/6 · `shapes2` 3/3 · `undo-redo` 1/1, 0 console. גיבויים: `backup-mobbar-shapefix-2026-07-25` · `backup-mergeundo-shapetab-2026-07-26` · `backup-cropresult-2026-07-26`). קודם — 2026-07-25ב (**3 תיקוני UX ממוקדים בסרגל הראשי/צף — כולם CSS-בלבד, בוצעו ישירות (לא דרך סאמר), אומתו חי + מרקוס**. המשך ללולאת-התיקונים של 2026-07-24 (בורר-צבעים/טאבים/אחידות-בית, ר' למטה). **(1) הסרת-צל לסרגל-הצף** — יקיר דיווח "יש לו צל מרוכך כל פעם שהוא נפתח"; אומת שזה `box-shadow` מכוון מעיצוב-Glass v2 (2026-07-02), לא באג. בשאלה מפורשת ביקש **להסיר לגמרי**. שורש-כפול: `--cb-shadow` (המשתנה שקובע את צל `.tb-body`, הכרטיס בפועל) ורגול-מאוחר-יותר בקובץ שדרס גם את הצל של `#ctxBar` עצמו עם `!important` — שניהם הוחלפו ל-`none`. אומת חי (`getComputedStyle` על שני האלמנטים = `"none"`), מרקוס `home-ribbon` 46/46. **(2) ביטול טאבים-צבעוניים** — יקיר בדק את הצבע-השונה-לכל-טאב שנוסף באיטרציה-2 (למטה) ולא אהב ("הצבעים לא עזרו, תחזיר לאפור"). הוסר לגמרי בלוק ה-CSS `[data-tab="X"]:not(.active)` הפר-טאבי; חזר לגוון-האחיד (אינדיגו 13%) שכבר היה קיים ומאושר מאיטרציה-1. אומת חי (`getComputedStyle` על 5 הטאבים = אותו `rgba(79,107,237,.13)`), מרקוס `home-ribbon` 46/46. **(3) הקטנת אייקוני "הוספה" שכולם ייכנסו בלי גלילה** — בקשת-משתמש מפורשת. נמדד חי: `ribbon.scrollWidth` של 20 כפתורי-insert (7 קבוצות) = **1401px**, חורג מרוחב-מסך טיפוסי → `overflow-x:auto` (שהוגדר במכוון ב-2026-07-04 כפתרון-נבחר-אז, "גלילה לא הקטנה") גורם לגלילה-אופקית כדי לראות הכל. זו **היפוך מכוון** להחלטת 2026-07-04 — נוסף בלוק-CSS חדש **אחרי** הבלוק ההוא (גובר בסדר-מקור): כפתור 60px→46px, אייקון 34px→22px, ריפוד/מרווחי-קבוצה צומצמו. `scrollWidth` ירד ל-**1173px** — נכנס בלי גלילה ברוב חלונות-דסקטופ. גלילה-אופקית נשארה כרשת-ביטחון למסכים צרים-מהרגיל בלבד. מרקוס `insert` 75/75, 0 שגיאות console._
>
> _קודם — 2026-07-25 (**תמונה — תיקון-שורש: אובייקט חופשי נתקע לצמיתות בגרירה לתחתית המסמך (§5/§15)**. יקיר דיווח שתי תלונות יחד: "התמונה לא שומרת על חוקי המסמך בחלק התחתון + לא עוברת בין שני הדפים" ו"אין סרגל צף לתמונה" — בבירור-מקדים (AskUserQuestion) התברר שזה קורה בחלון-דסקטופ רגיל (לא מובייל/מסך-צר — שולל את השערת `#mobBar`), וש"לא עוברת בין הדפים" פירושו בפועל **"נתקעת ולא זזה יותר כלל"**, לא חיתוך-חזותי. **אומת חי (לא רק קריאת-קוד):** הרצת `_onGlobalMove` אמיתית (לא סימולציית-מאוס, שלא-אמינה כש-rAF מושהה בחלונית-תצוגה לא-מוצגת — ר' [[rAF-paused-hidden-pane]]) על גרירת תמונה כלפי מטה במסמך בן 3 עמודים: התמונה עוברת חלק בין עמוד לעמוד (קפיצה נקייה מעל פס-הפער, כמתועד), אבל ברגע שהיא מגיעה לשוליים-התחתונים של **העמוד האחרון הקיים** — היא נעצרת ב-3292.21px ו**נשארת שם לצמיתות** גם כש ה ממשיכים לגרור עוד 600px נוספים. **שורש:** `_clamp`'s `docBot` (MyWord-v2.html~9239) נגזר מ-`page.offsetHeight` — גובה-המסמך *הנוכחי בפועל* (לא גדל אוטומטית). ב-Word, גרירת אובייקט צף מעבר לסוף המסמך **מוסיפה עמוד חדש אוטומטית** כדי לאפשר את ההמשך; ב-MyWord `_clamp` פשוט קפא במקום. **תוקן** (אישור-משתמש מפורש אחרי הצגת 3 אופציות): ב-`_onGlobalMove`'s ענף-הגרירה-החופשית (MyWord-v2.html~9645, **רק בגרירה חיה — לא בלולאת ה-`_clamp` הכללית של `updatePagination`**, כדי לא לגרום לצמיחת-עמודים בלתי-נשלטת בכל reflow/עריכת-תוכן) — לפני החלת `newT`, בודקים אם `newT + objH + margin > page.offsetHeight`; אם כן, מפעילים בדיוק את מנגנון כפתור "➕ עמוד" (`manualPages = effective+1; lastPages=0; updatePagination();`, מ-`addManualPage()` הקיים, MyWord-v2.html:5984) *לפני* ההצבה/`_clamp`, כך שהעמוד-הנוסף כבר קיים כש-`_clamp` רץ ואין תקיעה. **אומת חי אחרי-תיקון:** אותה גרירה (3 עמודים→8000px נוספים) — התמונה ממשיכה לזוז ברציפות לכל אורך הגרירה, המסמך גדל 3→6 עמודים אוטומטית בדיוק כשנדרש, `top` מגיע ל-6498px בלי שום עצירה. נבדק גם עם ריבוי-תמונות-ישנות (זיהום-בדיקה מ-IndexedDB) — קפיצה-אחורה חד-פעמית התבררה כהתנגשות עם אובייקט-בדיקה-קודם-שנשמר (מנגנון collision-avoidance הקיים, לא רגרסיה) ונעלמה לגמרי במסמך נקי. `#ctxBar` נבדק בנפרד ועובד תקין (display:flex, ממוקם בתוך ה-viewport) גם לאובייקט עמוק-במסמך-מורחב — ה"אין סרגל צף" לא שוחזר כבאג-נפרד; סביר שהיה תוצר-לוואי של חוויית-התקיעה. מרקוס `page-rules` **18/18** + `free-objects` **19/19** + `insert` **75/75**, syntax 0 שגיאות console — הכל זהה לבייסליין. גיבוי `backups/MyWord-v2.backup-dragautopagegrow-2026-07-25.html`.

**המשך אותו יום — באג-שורש שני, אמיתי: תמונת-inline נראית "מפוצלת לשתיים" בגבול-עמוד (`paginateBlocks`, לא `_clamp`).** יקיר שלח צילום-מסך שהראה תמונה שממש נראית חתוכה לשני חלקים על-פני "✕ סוף עמוד 1", עם תסכול מוצדק ("עושים אותו דבר ומתקנים אותו דבר"). **זה לא היה אותו באג כמו למעלה** — תמונה במצב `inline` (בשורה, in-flow) לא עוברת דרך `_clamp` כלל (§5 — `_clamp` מדלג על `float-left/float-right/inline`), אלא דרך `paginateBlocks()` (§4), שאמורה לדחוף אותה כיחידה אטומית "כמו כל בלוק" (כך אומר גם הקומנט הקיים בקוד). **שורש שנמצא ואומת חי:** הענף שמטפל ב-DIV/BLOCKQUOTE שחוצה גבול-עמוד (`paginateBlocks`, MyWord-v2.html~4811 — נוסף במקור כדי לטפל ברשימות מקוננות בתוך DIV מהדבקה) בדק רק `tagName==='DIV'`, בלי לשלול `.free-obj` — ותמונת-inline **גם היא DIV**. כתוצאה, הקוד קרא ל-`splitContainer(child)` על אובייקט-התמונה עצמו, שמתייחס לכל הילדים שלו (8 ידיות-resize, ידית-סיבוב, ידית-גרירה, `.obj-toolbar`, `.free-img-wrap`) כאילו הם "תוכן זורם" נפרד שאפשר לדחוף כל אחד לחוד במרגינים — **דחף חלק מהידיות (nw/n/ne + סיבוב) ב-margin-top עצמאי**, בעוד גוף-התמונה (`.free-img-wrap`) עצמו **נשאר במקום, חוצה את פס-הפער**, ונחתך שם ע"י ה-CSS mask (§3) — בדיוק החיתוך-לשניים שבצילום. אומת חי לפני-תיקון: תמונת-inline 300×400px שהוצבה לחצות את הפער — 4 מ-12 הילדים קיבלו `marginTop` שרירותי (5px/28px) בעוד `.free-img-wrap` נשאר ללא-שינוי; `objTop`/`objMarginTop` לא זזו כלל. **תוקן:** שני הענפים ב-`paginateBlocks` (DIV/BLOCKQUOTE ~4811, וה-fallback הכללי `isSplittable` ~4829) קיבלו guard מפורש `!child.classList.contains('free-obj')` — תמונת-inline נופלת עכשיו ישר ל-`extra = nextTop - top` הרגיל (בדיוק כמו פסקה/כותרת — יחידה אטומית, לא מיכל-לפיצול). אומת חי אחרי-תיקון: אותה תמונה (972px→972+400) נדחפת **שלמה** ל-1179px (ראש העמוד הבא), כל 12 הילדים חוזרים ל-offsets היחסיים המקוריים שלהם בלי אף margin עצמאי. מרקוס `page-rules` 18/18 + `insert` 75/75 + `free-objects` 19/19, syntax 0 שגיאות console — זהה לבייסליין. גיבוי `backups/MyWord-v2.backup-inlineimg-splitfix-2026-07-25.html` (מכיל גם את תיקון-הגרירה למעלה)._
>
> _קודם — 2026-07-24 (**סאמר — איטרציה 2: עיצוב הסרגל הראשי, 2 החלטות-משתמש מפורשות (Theme-Colors + טאבים-צבעוניים)**. המשך ישיר לאיטרציה 1 — שני הפריטים שנשארו פתוחים ("לא בוצע, ממתין-לאישור") קיבלו מהמשתמש שתי החלטות מפורשות, לא ניחושים. **(1) "צבעי ערכת נושא" (Theme Colors) בסגנון Word** — נוסף `THEME_COLORS` (10 צבעי-בסיס: 4 ניטרלים+6 גוונים-מזהים) + `_themeVariants(hex)` שגוזרת 5 גוונים **אלגוריתמית** (מיזוג-עם-לבן 80/60/40% ומיזוג-עם-שחור 25/50%, דרך `_hexToRgb`/`_rgbToHex` הקיימות — לא הוגדרו ידנית; שחור/לבן עצמם מקבלים 5 גוונים חד-כיווניים כמו ב-Word) בתוך `openColorWheel`: `.cw-theme-grid` חדש (CSS `grid-auto-flow:column`, 6 שורות לעמודה — כל צבע-בסיס+5-גווניו נכנסים לאותה עמודה לפי סדר-ההוספה). `.color-wheel` הורחב 232px→268px כדי להכיל 10 עמודות. **נוסף לפני** "צבעים סטנדרטיים" הקיים — לא נמחק כלום, "בנוסף" בדיוק כמו ב-Word (Theme+Standard+Recent). **ממצא-לוואי מדווח (לא תוקן, לא התבקש):** הפלטה השטוחה הישנה מאחורי ה-caret (`colorPalette`/`buildColorPalette`) התגלתה כ**קוד-מת בפועל** — מאזין-capture של `wireColorWheel` (2026-06-12c) עושה תמיד `stopPropagation` על ה-caret ופותח את `openColorWheel` במקומה; `pal.classList.toggle('open')` אף פעם לא מגיע להרצה. **(2) טאבים ראשיים — גוון-זיהוי שונה לכל טאב** — לפי בקשת המשתמש המפורשת נוסף `.tabs .tab[data-tab="X"]:not(.active)` per-tab (אותה עוצמה כמו הרגול הכללי מאיטרציה 1, גוון-שונה): `file`=טיל, `home`=אינדיגו, `insert`=ירוק, `layout`=כתום, `tools`=סגול, `view`=תכלת. הטאב הפעיל נשאר בגרדיאנט-האינדיגו הקיים בלי שינוי; hover משנה רק קו-תחתון (לא רקע), כמו הרגול הכללי; דארק/לילה/ניאון לא נגעו (ה-selector הקיים שם בעל ספציפיות-גבוהה-יותר וממשיך לגבור אוטומטית); לשוניות-הקשר (`imgformat`/`shapeformat`) לא מושפעות. אומת חי (Playwright screenshots+`getComputedStyle`) לכל שתי ההחלטות. מרקוס `home-ribbon` **46/46** + `insert` **75/75**, 0 שגיאות console. אין פתוח — שתי ההחלטות מומשו במלואן._
>
> _קודם — 2026-07-24 (**סאמר — איטרציה 1: עיצוב הסרגל הראשי, 3 תיקוני-אמת + סנכרון-תיעוד**. יקיר חזר על תלונה: "הסרגל לא יפה" (4 סעיפים, חלקם על תיקונים קודמים שלא הורגשו בפועל). **בדקתי חי בדפדפן (Playwright, לא רק קריאת-קוד) כל תלונה לפני שנגעתי בקוד — 3 מ-4 התבררו כתיקון-קודם-לא-אפקטיבי-בפועל, לא כתיקון-שלא-בוצע.** **(1) בורר-צבעים** — `openColorWheel`/`PRESET_COLORS` תוקנו כבר 2026-07-16 (4 עמודות+presets), אך ה-chips היו 18px (קטן ללחיצה מדויקת) וללא שם-צבע קריא (רק hex ב-title, כמו "#7f1d1d" ולא "בורדו"). נוסף `PRESET_COLOR_NAMES` (מפת 20 שמות עבריים, אותו דפוס כמו `bdrNames` הקיים למסגרות-תאים) המחובר גם לפלטה הבסיסית (`buildColorPalette`) וגם לגלגל המתקדם (`openColorWheel`) — עקביות בין שני המנגנונים; chips הוגדלו 18px→22px (`.color-wheel` 224px→232px); נוסף **Live Preview בהצבעה** (`mouseenter`/`mouseleave` על כל chip ב-presets/recents מעדכן `.cw-preview`+`.cw-hex` בלי להחיל בפועל — כמו Word, בלי צריך drag בגלגל כדי לראות). **(2) טאבים ראשיים** — תיקון 2026-07-16 (`rgba(79,107,237,.06)`) **קיים בקוד ועובד כמתוכנן**, אך אומת חי (`getComputedStyle`) שהוא כמעט בלתי-מורגש על-גבי זכוכית-הכותרת הבהירה (`rgba(255,255,255,.44)`+blur) — תיקון-לא-אפקטיבי-בפועל, לא כישלון-כיוון. **לא שונה הכיוון** (עדיין גוון-אחיד לכל הטאבים, לא צבעוני-לפי-טאב — שינוי-כיוון דורש אישור מפורש): רק חוזק bg .06→.13 + border .16→.34 + נוסף `border-bottom` (2px, כחול-אינדיגו) כעוגן-ויזואלי שלישי בלתי-תלוי-ברזולוציה. **(3) אי-אחידות בלשונית "בית" (ממצא חדש, #4):** נמצא שורש קונקרטי וממוקד — מתוך 5 קבוצות-כפתורים ב-home-ribbon רק "גופן" הייתה `.group-titled` (label+box, כמו כל שאר הסרגלים insert/layout/tools/view); "סגנון"/"פסקה"/"רשימות"/"בטל-בצע" היו `.group` שטוח בלי label — נראה כמו UI-legacy לצד UI-מודרני. הומרו כל 4 ל-`.group-titled` (rows/row+label, IDs/פונקציונליות ללא שינוי). **(4) "לא יפה" הכללי** — תוצר-נלווה של 1+2+3, לא שורש נפרד. אומת חי (screenshots+computed-style) לפני/אחרי כל תיקון; מרקוס `home-ribbon` **46/46** + `insert` **75/75** אחרי כל שינוי (3 ריצות), 0 שגיאות console. **המשך-לאיטרציה-הבאה (לא נבדק/לא-נסגר):** גלריית "Theme Colors" בסגנון-Word-מלא (עמודות=בסיס-צבע, שורות=tint/shade) — שינוי-מבנה-נתונים גדול יותר, לא בוצע כי לא ברור שזה הפער שהמשתמש מתכוון אליו; אם התלונה על הצבע נמשכת אחרי איטרציה זו, לשאול את המשתמש ספציפית מה עוד חסר. גיבוי לא נלקח (שינוי CSS/HTML ממוקד בלבד, אומת מלא מול מרקוס)._
>
> _קודם — 2026-07-21 (**qa-sweep loop — 2 באגי-אמת: XSS-הקשחה + חצים-מול-Delete**. לולאת-בדיקות (5 איטרציות, "לתקן קוד רק כשיש באג אמיתי"). **★★ XSS אמיתי (קדם-קיים, אומת חי):** הבדיקה `/^\s*javascript:/i` שהופיעה ב-5 אתרים (מחטא-הדבקה/ייצוא/ערוך-קישור) **ניתנת לעקיפה** — הדפדפן מסיר tab/LF/CR מכתובות לפני שהוא מפרש אותן, ולכן `href="java\nscript:alert(1)"` שרד את המחטא אך `a.href` התפרש כ-`javascript:`. הוכחה חיה: הדבקת `<a href="java\nscript:…">` דרך מסלול-ההדבקה האמיתי → `a.href="javascript:window.__XSS=1"`. תוקן: helper יחיד `_isDangerousUrl(v)` (מנרמל כמו הדפדפן — מסיר תווי-בקרה+רווחים ואז בודק `javascript|vbscript|livescript|mocha|data:`, עם `data:image/*` מותר) + `_isSafeUserUrl` (רשימת-היתר לכתובת שהמשתמש מקליד) ליד `_imgEnsureOrig`; כל 5 האתרים הוקשחו. suite `export-security`: `url-controlchar-scheme-bypass` (4 וקטורים + round-trip). **★ חצים-מול-Delete על אובייקט-נבחר (אישור-משתמש):** `editingText` המשותף מתעלם *במכוון* מסמן-מכווץ (נכון ל-Delete — אחרת אי-אפשר למחוק אובייקט-נבחר לעולם), אבל לחצים השיקול הפוך: חץ הוא מקש-ניווט-הסמן, וכשהסמן בטקסט הוא היה מזיז את הצורה במקום את הסמן. פוצל: `_caretInEditorText(o)` נפרד לחצים בלבד — סמן-מכווץ בעורך חוסם nudge; Delete לא נגע. **3 דקויות:** (1) בחירת-אובייקט (range על ההורה שמצביע על ה-`.free-obj`) **אינה** "סמן בטקסט" — אחרת קליק-על-צורה חוסם nudge; (2) פוקוס על ידית-האובייקט (⠿/resize/סיבוב) לעולם לא טקסט (`o.contains(activeElement)`) — אחרת `handle-kbd-move` נשבר כי הידית חולקת את המאזין הגלובלי; (3) `.tb-content` כן טקסט. אומת חי 5 מקרים; `kb-nudge-arrow-and-shift` הורחב עם מגן-רגרסיה. **גם:** תוקן באג-דטרמיניזם בבדיקות-rcmenu שלי (`styleWithCSS` הגלובלי → `execCommand('bold')` מייצר `<span style=font-weight>` במקום `<b>` בריצה-מלאה; המוצר תקין, הבדיקה בדקה תגית במקום אפקט — `__reset` מאפס styleWithCSS, האסרשן בודק font-weight). **רגרסיה מלאה: 1048/1048, 0 שגיאות console** (ריצה-נקייה ראשונה). ⚠️ לקח: אל תכתוב תווי-בקרה גולמיים ב-regex range — הופך את הקובץ לבינארי (קרה, זוהה ותוקן). גיבוי `backups/MyWord-v2.backup-bgcrop-2026-07-19.html`._
>
> _קודם — 2026-07-20 (**חיתוך-רקע-לאובייקט + תפריט-קליק-ימני: 5 תיקונים + suite חדש · ביקורת-תאימות**. בקשת-משתמש: "הסרת רקע — לתפוס את האובייקט ללא רקע ולהקטין או להגדיל" + ביקורת קליק-ימני/חוקה. **(א) חיתוך-לגבולות-האובייקט:** השורש הוא `.free-img-wrap img { object-fit: fill }` — התמונה תמיד ממלאת את ה-box, ולכן אחרי הסרת-רקע הידיות/הגרירה חבקו את **מלבן-המקור עם השוליים השקופים** ("תופסים אוויר"). נוספו `_imgOpaqueBounds` (סריקת alpha) + `_imgCropToBounds` + `_imgRestorePreCrop` ליד `_imgEnsureOrig`, ותיבת-סימון "✂️ חתוך לגבולות האובייקט" ב-`openBgRemovePanel` (דלוקה כברירת-מחדל, פועלת ב"החל"). **שני דברים שבלעדיהם זה נשבר:** (1) חיתוך-פיקסלים לבדו מותח את התמונה — חייבים לכווץ `width/height` באותו יחס ולהזיז `left/top` ב-`W*x0/nw`; לאובייקט **מסובב** מרכז-החיתוך עובר את אותה רוטציה סביב המרכז הישן, אחרת הוא קופץ בחיתוך א-סימטרי. (2) "שחזר תמונה מקורית" חייב להחזיר גם גיאומטריה — **3** מסלולי-שחזור (פאנל-מסננים/פאנל-רקע/ריבון `imgformat`) קיבלו `_imgRestorePreCrop`; הגיאומטריה ב-`dataset.preCropBox`, מסוריאליזת כ-`imgPreCrop`. אומת חי: תמונה 100×100 עם ריבוע ב-(30,20,40,25) → פיקסלים 40×25, box 40×25, left/top 68/58 — בדיוק הצפוי. **(ב) תפריט קליק-ימני — 5 תיקונים:** **★★ P1 (הכי חמור)** `e.preventDefault()` הגורף חסם את **תפריט-הדפדפן**, שהוא המסלול **היחיד** להצעות-איות (`spellcheck` נייטיב, אין מנוע-איות משלנו) — הקו-האדום הופיע ואי-אפשר היה לתקן דרכו. תוקן: סמן-מכווץ בטקסט רגיל → תפריט-הדפדפן (וגם הדבקה מלאת-עיצוב בחינם); בחירה/תא-טבלה/קישור → התפריט שלנו; כיבוי `#tlSpell` מחזיר את התפריט שלנו גם על סמן-מכווץ. **★ P2** ה-guard `closest('.free-obj')` בלע גם את `.tb-content` — עריכת-טקסט אמיתית בתיבה נשארה בלי שום תפריט; נוסף חריג (תמונה/צורה עדיין בסרגל-הצף, במכוון). **P4** נגישות APG: `role=menu/menuitem/separator` + ניווט-חצים/Home/End/Enter — ההדגשה ב-**class + `aria-activedescendant`** ולא ב-`focus()`, כי הזזת-הפוקוס אל הכפתור מוציאה אותו מה-contenteditable ושוברת את `execCommand` (בדיוק הסיבה ש-mousedown עושה preventDefault). **P5** קליק-ימני על `<a>` הציג "הוסף קישור"; הוחלף בפתח/העתק-כתובת/ערוך/הסר (עם חסימת `javascript:`). **P6** מיקום היה LTR — עכשיו הקצה הימני בסמן וגדל שמאלה; מקש-התפריט של Windows (clientX/Y=0) נפל לפינה ועכשיו נופל למלבן-הבחירה. **suite חדש `rcmenu` 7/7** (היה **אפס** כיסוי ל-138 שורות) — **הוכחת-אדום:** מול הגיבוי שלפני-התיקון **2/7 עם 15 ממצאים**. `__reset` מנקה כעת גם `#rcMenu` (singleton ב-body, לא `.mw-panel` — אותה מלכודת-בידוד כמו `#bookmarkNavPanel`). **(ג) ביקורת-תאימות:** **לא נמצאה אף הפרת-חוקה בקוד** (A4/pagination/`_clamp`/IMG_MODES/2-שורות/Paper.js/קיצורים — כולם אומתו). תוקן **סחף-תיעוד**: `docs/CODE-INDEX.md` סונכרן מחדש (`rebuild-code-index.mjs`, 113 שורות — `_clamp` היה רשום 5511 ובפועל 9086) + הוסרה אזהרת-"מיושן"; **2 בלוקי-הערה בקוד** שתיארו את מדיניות "אובייקט שייך לעמוד אחד" מ-07-11 (שבוטלה ב-07-13) הוסרו — הם סתרו את הקוד שמתחתיהם. `IMAGES.md`/`§7 IndexedDB`/`§2 ניאון`/`check_syntax.mjs` נמצאו **כבר מתוקנים** (הדו"ח היה מבוסס-קריאה-ישנה — אומת ידנית מול הקבצים). רגרסיה: `insert` 75/75 · `free-objects` 19/19 · `page-rules` 18/18 · `home-ribbon` 46/46 · `keyboard` 20/20 · `rcmenu` 7/7, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-bgcrop-2026-07-19.html`._
>
> _קודם — 2026-07-19 (**ביקורת-סשנים + תיקון קישור-התיבות + סנכרון-תיעוד**. בוצע מיפוי של כל שרשרת-הגיבויים מול הקוד החי: **אין רגרסיה** — כל העוגנים ההיסטוריים שרדו, כל המחיקות היו מכוונות. **★ באג-אמת שנמצא באימות-חי:** קישור-תיבות-הטקסט (2026-07-18) היה **חסר-השפעה בשקט** בברירת-המחדל — תנאי-הזרימה `scrollHeight>clientHeight` לא מתקיים כש-`tbAutofit='auto'` (`overflow:visible`, התיבה גדלה עם התוכן; נמדד `ch=480,sh=480`). תוקן: `_tbFinishLinkPick` מעביר תיבת-מקור ל-`fixed` אוטומטית + `showHint` מודיע. אחרי: `ch=88,sh=480`, 11 בלוקים זרמו בסדר נכון. **סונכרנו 4 סשנים שלא היו ב-CLAUDE.md:** טבלה AutoFit/סגנונות/מיון/כותרת-חוזרת (07-18/19), תמונה סגנונות-מוכנים+דחיסה (07-15/18), קישור-תיבות (07-18, **הופך את ההחלטה "דולג במכוון" מ-07-16ב** — ההנחה "דורש שינוי בליבת ה-pagination" התבררה כשגויה, `.tb-content` מחוץ-לזרם). **סשן לא-משוחזר:** גיבוי `backup-remove-ghost-toolbar-2026-07-16.html` (16/07 21:24) — ה-diff מולו מכיל מחיקה אחת בלבד (הערת-קוד), לא ניתן לשחזר ממנו מה בוצע; מדווח ולא נכתב-בהשערה. מרקוס `insert` 75/75 + `free-objects` 19/19, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-pre-tblink-autofit-20260719-215309.html`._
>
> _קודם — 2026-07-18 (**סגירת-פערי-בדיקות + 3 באגי-אמת שנמצאו תוך כדי — qa-flow מלא (בדיקה-אדומה לפני תיקון)**. הרקע: אימות מול הקוד החי גילה ש-`docs/FEATURES-INDEX.md` (10/07) מיושן — 2 פערים כבר נסגרו (`ctx-tabs`, `export-security`), 2 חלקית (`img-edit` בלי פאנל-מסגרת, `calculator` 4/9 מצבים), 7 עדיין פתוחים. **נוספו 14 תרחישים אמיתיים** (לא "האלמנט קיים"): `shape-merge` 3 (unite/subtract/exclude — מפרק את ה-SVG ומוודא `<path>` אמיתי מ-Paper.js ולא נפילה ל-`<mask>` fallback), `snippets-security` 2 (הזרקת `<img onerror>`/`<script>` לשם-קטע — התרחיש הקודם `tools-snippet-save` **לא בדק XSS כלל**), `doctabs` +4 מקרי-קצה, `bookmarks` 2, `a11y-keyboard` 2, `calculator` +2. **★★ 5 באגי-אמת נתפסו ותוקנו** (כולם היו "מתועדים כעובדים"): **(1+2) "החייאת מסמך"** — `newDoc()` שומר את המסמך-הנוכחי אם `currentDocId` מוגדר; גם `openDocManager`'s מחיקה וגם `closeTab()` קראו לו **לפני** איפוס ה-id → המסמך שזה-עתה נמחק/נסגר נשמר בחזרה תחת אותו id ("מחק" לא מחק כלום). תוקן: `currentDocId = null` לפני `newDoc()` בשני האתרים. **(3) מחיקת-סימנייה מחקה טקסט (איבוד-תוכן)** — סימנייה-טווח נוצרת ב-`range.surroundContents(span)`, כלומר ה-span **עוטף את טקסט-המשתמש**; `m.remove()` מחק אותו יחד איתו (סימון "עולם"+מחיקה = המילה נעלמה). תוקן ל-unwrap (העברת ילדים החוצה + `normalize()`); `bm-point` (תו-אפס-רוחב בלבד) נשאר `remove()`. **(4) מצב-משתנים במחשבון היה קוד-מת** — קוד-ההצבה לפני `safeEval` תקין, אך מקלדת-המחשבון מכילה ספרות/אופרטורים בלבד ומקש `x` ממופה בכוונה לכפל → שם-משתנה לא יכול היה להיכנס לביטוי. תוקן: לחיצה על צ'יפ מזריקה בדפוס קבוע-מדעי (π/e) — השם ל-`expr`, הערך ל-`rawExpr`, כך `safeEval` (ספרות בלבד) לא נגע; ✕ הופרד לכפתור נפרד. **(5) ידיות-הטבלה בלתי-נגישות למקלדת** — `tabIndex=0`+`ArrowUp/Down` היו כתובים, אך `position()` נקרא **רק מ-`mousemove`** → `display:none` לתמיד ללא עכבר, ואלמנט `display:none` מחוץ לסדר-הטאבים (וגם `curTable` לא נקבע). תוקן: `selectionchange` (כניסת-סמן-לטבלה = המקבילה ל-hover) + focus/blur נחשבים כמו hover. **הוכחת-אדום מפורשת:** נבנה עותק-זמני עם התיקונים מוסרים — מחשבון החזיר `"0 + 0 = 0"` במקום 8 והצ'יפים מחקו את עצמם, ידית-הטבלה `display:"none"` ולא-ממוקדת; אחרי התיקון הכל ירוק. גם נתפסה **מלכודת-בידוד בבדיקות** (לא באג-מוצר): `#bookmarkNavPanel` אינו `.mw-panel` ולכן `__reset()` לא מסירו, ומחיקת-סימנייה לא סוגרת אותו → הקליק בתרחיש-הבא *סגר* פאנל-פתוח; נוסף `window.__openBookmarkPanel()`. רגרסיה מלאה ירוקה: `insert` 75/75 · `page-rules` 18/18 · `home-ribbon` 46/46 · `keyboard` 20/20 · `free-objects` 19/19 · `storage` 12/12 · `doctabs` 9/9 · `calculator` 7/7 · `print` 10/10 · `ctx-tabs` 6/6 · `tools/view/layout-ribbon` 6/5/6 · `img-edit` 4/4 · `export-security` 4/4 · + כל החדשים — **0 שגיאות console בכל הסוויטות**. גיבוי `backups/MyWord-v2.backup-3bugfix-bookmark-calcvars-tbla11y-2026-07-18.html`. **סבב-המשך (אותו יום) — נעילת-רגרסיה ל-8 פיצ'רים שאומתו-תקינים אך היו ללא כיסוי:** `calculator` +5 (המרת-יחידות 5ק"מ=5000מ' ו-100°C=212°F כדי לנעול את מקדמי-ההמרה ואת מסלול-ה-special של הטמפרטורה · סטטיסטיקה על סדרה ידועה — ממוצע/חציון/טווח · ריבועית x²−5x+6 → 3,2 · פיתגורס 3,4→5) → `calculator` **12/12**; `img-edit` +1 (פאנל-מסגרת/פינות — החלה בפועל על `.free-img-wrap` + round-trip של `imgBorder`/`imgBorderRadius`) → **5/5**; suite חדש `sidebar-night` 2 (❄️ מפעיל `data-theme="night"` ומציג `#mwSidebar`, **והדף נשאר לבן** לפי §view · מתאר-המסמך מתעדכן מ-h1/h2). **אף שורת-מוצר לא שונתה בסבב הזה — בדיקות בלבד.** נתפסה גם **בדיקה שגויה משלי** (לא באג-מוצר): `textContent` של בלוקים סמוכים נדבק (`"= 3x₂"`), ולכן `\b3\b` נכשל למרות שהפלט נכון — הוחלף בחילוץ-ערך ממוקד (`x₁…=…=(\d)`). **פתוח (לא נבדק, מדווח):** TTS ללא בדיקת-network-payload (אומת ידנית שהקצב נשלח בפועל לשרת, אך ללא תרחיש-קבע), הכתבה/איות לא-ניתנים-לבדיקה ב-headless (יש fallback תקין בקוד). ~~**הערת-תשתית:** `qa/check_syntax.mjs` מצביע לנתיב שגוי~~ — **האבחנה הייתה שגויה והפריט נסגר 2026-07-19:** הבעיה לא הייתה נתיב-חסר אלא `path.resolve` יחסי-ל-**cwd** (עבד מ-`qa/`, נכשל משורש-הפרויקט). תוקן ל-`path.resolve(__dirname, …)`; בנוסף הוסרו 2 בדיקות-מתות (`startPresentation`/`buildSlides` — המצגת הוסרה 2026-06-12b) והוחלפו בבדיקות-שפיות חיות._
>
> _קודם — 2026-07-16ב (**סאמר — הרחבת תיבת-טקסט חופשית: 8/10 פערים מול Word/Google Docs/Canva ממומשים במלואם, add-only** — יזום ע"י המשתמש: השוואת יכולות `.free-obj.free-textbox` (`insertFreeTextbox`/`buildTextbox`) לעורכים המקבילים. **8 פיצ'רים חדשים**, כולם בעמוד-2 של "עוד" ב-ctxBar (חוק 2-השורות §8 נשמר, שום שינוי בשורה הראשית הקיימת): **(1) מסגרת מתקדמת** — `openTextboxBorderPanel` (עובי/סגנון/צבע), כותב ישירות ל-`obj.style.border`/`borderStyle` הקיימים (`tbBorder`/`tbBorderStyle` כבר בסריאליזציה — אין הגירת-סכמה). **(2) מרווח פנימי** — `openTextboxPaddingPanel`, שדה `tbPadding` חדש. **(3) יישור-טקסט אנכי** — `window._setTbValign(obj,'top'|'middle'|'bottom')` דרך flex על `.tb-content`, שדה `tbValign`. **(4) Autofit 3-מצבים כמו Word** — `window._setTbAutofit(obj,'auto'|'fixed'|'shrink')`: `fixed`/`shrink` הופכים את התיבה ל-`flex` עם `.tb-content{flex:1;overflow:hidden}` (גלישה נחתכת בלי מדידת-JS בכל frame); `shrink` מוסיף גם כיווץ font-size הדרגתי (`_tbShrinkToFit`, עד 8px) על `input` נפרד (לא מתנגש ב-`scheduleSave` הקיים). שדה `tbAutofit`. **(5) כיוון-טקסט אנכי** — `window._toggleTbVertical(obj)` (`writing-mode:vertical-rl`), שדה `tbVertical`. **(6) מיקום/גודל/זווית מדויקים** — **שימוש-חוזר** ב-`openShapeTransformPanel` הגנרי הקיים (עובד על כל `free-obj`) — אפס קוד חדש. **(7) צל** — `openTextboxShadowPanel`+`window._applyTbShadow` (`box-shadow` על ה-`obj` עצמו, לא `filter` על wrap כמו בצורות — תיבת-טקסט היא div מלבני רגיל), שדה `tbShadow` (JSON). **(8) Format Painter לתיבה** — `window._captureTbStyle`/`_applyTbStyle` (רקע/מסגרת/צל/שקיפות/padding — לא מיקום/גודל/תוכן), אותו דפוס בדיוק כמו `_captureShapeStyle`/`_applyShapeStyle`. **(10) צורת-מיכל ויזואלית** — `openTextboxClipPicker`+`window._applyTbClip` (`clip-path`: אליפסה/עיגול/מעוין/משושה/מקבילית; ה-bounding-box/text-flow נשארים מלבניים לגמרי — לא `shape-outside`, בכוונה, לפי הנחיה מפורשת). כל הפאנלים בדפוס `.mw-panel` הזהה לפאנלי-הצורות, ממוקמים דרך `_shapeFxPanelPlace` המשותף (ליד `openStrokePanel`/`openShapeShadowPanel`, ~שורה 11266). **(9) קישור בין תיבות (זרימת-טקסט אוטומטית) — דולג במכוון, out-of-scope מדווח:** דורש שינוי בליבת ה-pagination/save (חישוב-overflow על כל input + פיצול-DOM + עדכון-שרשרת + אינטגרציה עם undo/`_clamp`/`updatePagination` לכל תיבה בשרשרת) — סיכון-רגרסיה לא-מוצדק לפיצ'ר לא-מבוקש-מפורשות; גם "גרסה מצומצמת" (כפתור-ידני) נדחתה מאותה סיבה. שדות-סריאליזציה חדשים (`tbPadding`/`tbValign`/`tbAutofit`/`tbVertical`/`tbShadow`/`tbClip`) — כולם ברירת-מחדל ריקה/false, מסמכים ישנים נטענים ללא שינוי (`if(d.tbXxx)` guard בכל מקום, כמו שאר השדות האופציונליים). אומת חי (Playwright headless, לא syntax-בלבד): פתיחת כל פאנל + החלה בפועל + round-trip מלא `_serializeObj`→`_deserializeFreeObjs` לכל 8 השדות, 0 שגיאות console. מרקוס `insert` **75/75** + `free-objects` **14/14** + `page-rules` **18/18** — כולם זהים לבייסליין (0 רגרסיה). גיבוי `backups/MyWord-v2.backup-textbox-expand-2026-07-16.html` (+ גיבויי-ביניים עקב הפסקת-רשת באמצע הסשן — התיקון הושלם בסשן-המשך על הקובץ החי, לא נדרס אף שינוי-a11y/ribbon-split מקביל). מפרט מעודכן: `docs/SPEC-INSERT-TEXTBOX.md`. פירוט: wiki/log.md "2026-07-16ב"). קודם — 2026-07-16 (**סאמר — 4 תיקוני UX/UI מ-`docs/SPEC-DESIGN-RIBBON.md` (נוי), אישור-משתמש מפורש: "רק לתקן את הקוד, לא לגעת במה שעובד"**. **(1) בורר-צבעים (§3, קריטי):** CSS `.color-palette .grid { grid-template-columns: repeat(8, 22px) }` שבר גריד לוגי של 20 צבעים (5 שורות×4) ל-8/8/4 (שורה שלישית חלקית) — תוקן ל-`repeat(4, 26px)` + swatch 26px; הוסר צבע-כפול `#4F6BED` (הוחלף ל-`#0ea5e9`); שורת "צבעים סטנדרטיים" (`PRESET_COLORS`) נוספה בתוך `openColorWheel()` עצמו — ה-CSS `.cw-presets` היה **כבר קיים ולא בשימוש** (חיבור נשכח), כעת ה-caret ▾ מציג Theme+Standard+Recent במקום אחד כמו Word, בלי למחוק אף מנגנון קיים. **(2) `makeStyleChip()` לסרגל-הצף (§6):** הפונקציה+CSS+פאנל `#ctxStylePop` היו קיימים במלואם (גם `updateStyleChip()` כבר נקרא ב-2 מקומות) אבל אף קריאה ל-`cmds.appendChild(makeStyleChip())` לא הייתה ב-`buildText()` — תוספת-שורה-אחת, מאפשרת להפוך פסקה לכותרת מהסרגל הצף בלי לעבור ל"בית". **(3) ribbon-split לקבוצות-ענק (§5):** דפוס `.ribbon-split`/`.ribbon-caret` הקיים כבר בלשונית "הוספה" (reuse מדויק, לא מנגנון חדש) הורחב ל"יישור בעמוד" (6→1+caret, imgformat+shapeformat), "סיבוב"/"סיבוב מהיר" (3→1+caret, שניהם), "מיזוג צורות" (4→1+caret, shapeformat — כפתור-ראשי חוזר על הפעולה האחרונה כמו `insShape`); `splitGroup()` helper חדש ב-`initCtxFormatTabs`. **(4) טאבים ראשיים (§4):** a11y — `role="tablist"` על ה-`<nav>` + `role="tab"`+`aria-selected` דינמי על כל טאב (חסר לגמרי, כמו לשוניות-המחשבון שכבר תקינות); עיצוב ("אופציה A" שמרנית, אושרה) — גוון אפור-כחלחל עדין (`rgba(79,107,237,.06)`) לטאבים-לא-פעילים (לא צבע-שונה-לכל-טאב — נדחה), `:not(.ctx-format-tab)` כדי לא לפגוע בזהות-הענבר הקיימת של לשוניות-הקשר תמונה/צורה; הטאב הפעיל (גרדיאנט אינדיגו) לא נגוע. אומת חי (Playwright) לכל 4: פלטה מיושרת ל-4 עמודות, style-chip פותח ומחיל H1, 3 קבוצות-ribbon-split מציגות תפריט ומפעילות את אותן פונקציות כמו קודם (align/rotate/merge — מיזוג עם ✓ על הנוכחי וכפתור-ראשי חוזר על האחרון), aria-selected מתעדכן נכון בלחיצה ו-activeBg נשאר גרדיאנט. מרקוס `insert` 75/75 + `page-rules` 18/18 + `home-ribbon` 46/46 + `view-ribbon` 5/5 — כולם זהים לבייסליין, 0 שגיאות console. **לא בוצע (out of scope):** שאריות-כתום `#ctxStylePop`/`#ctxSizePop` (§2 הישן), גובה-שורה בבורר-הצורות (§1 הישן) — נמוכי-עדיפות, לא נגזרו מתלונות 2026-07-15/16. פירוט: wiki/log.md "2026-07-16"). קודם — 2026-07-14ב (**טבלה — תיקון-שורש ל-Resize: עוגן בצד הנגדי, בלי מירכוז ובלי Scale** — בקשת-משתמש מפורשת אחרי בדיקה ידנית: "גוררים ידית ימנית → רק גבול ימני זז, שמאלי קבוע" (ולהפך), "בפינות רק שני צדדי-אותה-פינה זזים", "אסור להזיז את כל הטבלה", "אסור לבצע Scale" — כמו Word/Figma. **שורש כפול:** (1) גרירת-קצה-חיצוני של עמודה (`_colDrag`) קבעה `marginInlineStart`+`marginInlineEnd` לאותו ערך שלילי-סימטרי בלי תלות באיזה צד נגרר → מירכוז שהזיז גם את הצד-הנגדי ("אני גורר וכל הטבלה זזה"). (2) ידית-הפינה (↘, מ-2026-07-12ב) הפעילה `scaleX`/`scaleY` אחיד על **כל** העמודות/שורות — Scale אמיתי, לא "רק שני צדדי-הפינה". **תוקן:** `edge==='right'` (עמודה 0) — `marginInlineStart = startMargin - deltaW` (דלתא-רוחב מדויקת מתחילת-הגרירה, לא "half" סימטרי) כדי שה-start (ימין ב-RTL) יזוז וה-end (שמאל) יישאר קבוע; `edge==='left'` — ברירת-המחדל של קופסת-CSS כבר מעגנת נכון, לא נוגעים ב-margin כלל. `maxTableW` שונה מ-`contentW+2*overhang` (סימטרי) ל-`contentW+overhang` (חד-צדדי). ידית-הפינה: `_applyTableSize` (scale-אחיד) **הוסרה לגמרי**, הוחלפה ב-`_applyCornerColWidth`/`_applyCornerRowHeight` — משנות **רק** עמודה 0 (הימנית-ביותר) ו**רק** השורה האחרונה, הרכבה של אותה נוסחת-עוגן + `_rowDrag`-הקיים; מסלול-מקלדת (Arrow×4) שוכתב באותה לוגיקה. אומת חי (Playwright, מדידות-פיקסל): גרירת-ימין +80px → left קבוע-בדיוק (587→587), right זז +80; גרירת-שמאל -60px → right קבוע-בדיוק, left זז -60; פינה (dx=+70,dy=+30) → עמודה-0 בלבד +70, עמודות-אחרות זהות-ביט (0 Scale), שורה-אחרונה בלבד +30, שורות-אחרות זהות, left/top הטבלה קבועים-לגמרי; גבול-פנימי+גבול-שורה (לא נגעתי) עדיין תקינים. מרקוס `insert` 75/75 + `page-rules` 18/18, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-tblresize-anchor-2026-07-14.html`. פירוט: wiki/log.md "2026-07-14ב"). קודם — 2026-07-14 (**צורות — סדר-שכבות מדורג כמו Canva + "החלף צורה"** — בקשת-משתמש בעקבות ה-qa-sweep הקודם ("כל פונקציות אלו צריכות להיות בסרגל הראשי כולל אפשרות מיקום של שכבה של צורה מה מעל מה כמו בקנבה... יש עוד דברים בקנבה שאפשר לשפר, תוסיף מבלי לתקן את מה שקיים"). נבדק מול Canva (WebSearch): 4 פעולות-שכבה — Forward/Backward (**צעד אחד**) + To-Front/To-Back (**קפיצה לקצה**) — MyWord הכיל רק את הזוג השני (`_fwdZ`/`_bwdZ` בסרגל-הצף, `zFwd`/`zBwd` בריבון), בלי צעד-אחד אמיתי. **(1)** `_stepZOrder(obj,dir)` — פונקציה גלובלית חדשה (ליד `_objsAtPoint`, ~8967) ממיינת את כל `.free-obj` לפי z ומחליפה עם השכן הקרוב (שובר-שוויון ב-z זהה); משותפת לריבון+סרגל-צף כדי שלא תיכפל-לוגיקה. **חיווט additive בלבד — לא נגעתי בכפתורי הקפיצה-לקצה הקיימים:** ריבון `shapeformat`/`imgformat` (קבוצת "סידור", `layerBtns`) קיבל 2 כפתורים חדשים (🔺קדימה שכבה-אחת / 🔻אחורה שכבה-אחת) בין ה-🔼/🔽 הקיימים; הסרגל-הצף קיבל אותם 2 פריטים ב-`layerMoreItems` (המשומש כבר ע"י תיבת-הטקסט) + הוזרקו גם ל"עוד" של תמונה/צורה (`buildImage`, שלא השתמש ב-`layerMoreItems` קודם) — נשמר חוק-2-השורות (§8) כי הכל הלך ל"עוד", לא לשורה הראשית. **(2) "🔄 החלף צורה" (Replace shape)** — תוסף חדש בהשראת Canva "Edit shape": `openShapeReplacePicker(anchor,obj)` (ליד `openShapePicker`, ~10707) — פותח את אותו בורר-קטגוריות (גאומטריות/מיוחדות/חצים/דיאגרמות, ללא "מחברים" — מודל-אובייקט שונה) אך במקום להכניס צורה חדשה קובע `obj.dataset.shapeKind` ומרנדר-מחדש; בטוח כי `_renderShape` בונה תמיד רק מ-`dataset.shapeKind`+שדות-הסגנון הקיימים (fill/stroke/strokeW/dashStyle/cornerRadius) — צבע/קו/מיקום/גודל/סיבוב נשארים כשהם. כפתור ב-ריבון (קבוצת "סגנון צורה") + ב"עוד" של הסרגל-הצף (לא בשורה הראשית). אומת חי: `_stepZOrder` מחליף שכנים נכון (A↔B), no-op בקצה; ריבון-קליק אמיתי מחליף z; replace `rect→star5` שומר fill/stroke/מיקום/גודל/סיבוב בדיוק, פאנל נסגר אחרי בחירה. מרקוס `insert` 75/75 + `free-objects` 14/14, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-shapelayer-2026-07-14.html`. **נשקל ולא-מומש (out of scope, לא ביקשת):** תגית-חצים דו-כיוונית/עצמאית לקווים (connArrow יש רק חץ-קצה-יחיד קבוע), פאנל-שכבות מלא (רשימה נגררת כמו Canva Pro) — Canva Free עצמו לא כולל את זה. פירוט: wiki/log.md "2026-07-14"). קודם — 2026-07-13ב (**טבלה: בורר-גלישה (כמו תמונה) + ידיות-גרירה שלא-נעלמות** — בקשת-משתמש בעקבות ה-qa-sweep. **(1)** כפתור-הגלישה המחזורי (block→float-right→float-left בלחיצות) הוחלף בפאנל-בורר `openTableWrapPanel`+`TABLE_WRAP_MODES` (3 מצבים: רוחב-מלא/עוטף-ימין/עוטף-שמאל), ליד `openWrapPanel` של התמונה ובאותו CSS `.wrap-panel`+listeners גלובליים (`closeWrapPanel`) — הטבלה מקבלת בדיוק את חוויית בורר-המצבים של התמונה (אייקונים, active מסומן, Escape+קליק-חוץ); כל מצב קורא ל-`_setTableWrap` הקיים; כפתור-הגלישה ב-`buildTable` פותח פאנל עם `▾`. **(2)** ב-`initTableMove`, מאזין ה-`mousemove` קרא `hide()` **מיד** בעזיבת-תא — הידיות (⠿ הזזה + ↘ resize מ-2026-07-12ב) ~6px מימין לטבלה, והרווח ביניהן הבליע את הידית לפני שהעכבר הגיע אליה ("תמיד נעלם שזזים מטבלה" / "אפשרות לגרור לא מופיעה"). תוקן עם `scheduleHide()`/`cancelHide()` (טיימר 320ms) — `mouseenter` של כל ידית וחזרה-לתא מבטלים; `onUp`/`onResizeUp` הוחלפו מ-`hide()` ל-`scheduleHide()`; נתיב הגרירה/reorder/resize לא נגע. אומת חי (פאנל 3-מצבים+Escape+float-right מוחל; ידיות נשארות בעזיבה, נשמרות בהגעה, מוסתרות אחרי-טיימר; reorder עדיין עובד) + מרקוס `insert` 75/75 + `page-rules` 18/18, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-tblwrap-drag-2026-07-12.html`. פירוט: wiki/log.md "2026-07-13"). קודם — 2026-07-13 (**qa-sweep צורות — סגירת 3 פערי-ריבון + הבהרת חיתוך (העליונה חותכת את התחתונה)**: לפי הכרעות-משתמש בגייטים של qa-flow. **(א) 3 פערי ריבון `shapeformat` נסגרו** (היו קיימים רק בסרגל-הצף): קבוצת "סיבוב מהיר" (↺/↻ 90° + ⊙ אפס — אותה מכניקה `dataset.rotation`+`transform` כמו "עוד" של הסרגל הצף) + קבוצת "שקיפות ונעילה" (מחוון-שקיפות זהה ל-`addOpacityCtrl` + כפתור נעל/שחרר זהה ל-`addLockBtn`); `syncShapeControls(o)` חדש (נקרא מ-`_ctxFormatTabsUpdate` ליד `syncImgControls`) מסנכרן שקיפות+אייקון-נעילה עם הבחירה. **(ב) חיתוך צורות הובהר** (בקשת-משתמש מפורשת: "העליונה חותכת את התחתונה, שיהיה ברור ומובן"): `combineSelectedShapes` ממיין את הצורות לפי z עולה (`shapes.sort`) → בסיס-החיתוך שנשמר = הצורה **התחתונה** (z נמוך), הצורות שמעליה חותכות ממנה; התוצאה יורשת את סגנון התחתונה (`paths[0]`). `buildMulti` מסמן `.multi-anchor` על הצורה-התחתונה (לא הראשונה-שנבחרה) + CSS חדש בסוף `<style>`: `outline` ירוק (#16a34a) + badge "בסיס ✓ נשאר" (`::after`). tooltips של ✂️ עודכנו בשני המקומות (`buildMulti` + ריבון). אומת חי: subtract עם input הפוך שומר את התחתונה (green, לא red); anchor=bottom+outline ירוק; 3 קבוצות-הריבון עובדות (opacity 50%→30%, rotate 0→-90°, lock toggle+icon 🔒→🔓). מרקוס `insert` **75/75** + `free-objects` **14/14**, syntax 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-shapefix-2026-07-12.html`. **(ג) #1+#6 — מעבר-חופשי-בין-עמודים לאובייקטים חופשיים** (המשך, אישור-משתמש נוסף באותו יום — **היפוך §5**): `_clamp` (~9001) שוכתב — אנכית מוגבל לגבולות-המסמך בלבד (`docTop`/`docBot`, `H=editor.offsetHeight`) במקום נעילה-לעמוד-הכיל, כך שאובייקט מגיע לכל עמוד (#1). **הרחקה-מפס-הפער** חדשה: כי ה-CSS mask חותך את הפער (צורה שם נראית "יוצאת מהדף" — #6), אם ה-bbox חוצה פס-פער הוא נדחף מינימלית החוצה (למעלה/למטה, הקרוב) → לעולם לא נחתך. `_clampFloat` (תמונות-גלישה) **לא** שונה (מעוגן-לטקסט). אומת חי: T2 מגיע לעמוד-3, T1/T3 נדחפים מהפער (bottom→1122, top→2358), T4 חסום לתחתית-מסמך; מרקוס `insert` 75/75 + `free-objects` 14/14 + `page-rules` 18/18, 0 console. §5 עודכן. **(ד) #2 — בחירת צורה שמתחת לאחרת** (Alt+לחיצה): helper `_objsAtPoint` חדש (כל האובייקטים בנקודה, z יורד) + ענף-Alt במאזין-הבחירה (capture, ~13966) — "מקלף" מטה דרך ערימת-החפיפה. בבחירה-בודדת בוחר את שמתחת לנבחרת; בבחירה-מרובה מוסיף אותה (`stack.slice(1)` דילוג-על-העליונה). אומת חי (single: top→T, Alt→B; multi: shift→T, Alt→+B) + מרקוס insert 75/75 + free-objects 14/14, 0 console. **כל 6 ליקויי-הצורות + 3 פערי-הריבון טופלו.** פירוט: wiki/log.md "2026-07-13"). קודם — 2026-07-12ב (**ידית resize-פינתית לכל הטבלה — סוגר את פער-Word האחרון מ-qa-sweep**: `.tbl-resize-handle` (↘) חדשה ליד ⠿ הקיימת ב-`initTableMove` — גרירה קוראת ל-`_applyTableSize(table,targetW,targetH)` חדש, מגדילה/מקטינה **את כל** העמודות+השורות בסקאלה אחידה (בניגוד ל-`_colDrag`/`_rowDrag` שמזיזים גבול-בודד), עם אותה חסימת-`maxTableW`+מרכוז כמו גרירת-קצה-חיצוני; נגיש-מקלדת (Arrow×4 על הידית, צעד 0.2 ס"מ). **★ באג-אמת נתפס באימות-חי ותוקן לפני-סגירה:** גרסה ראשונה קראה רוחב-כל-עמודה *בתוך* הלולאה *אחרי* שעמודה קודמת כבר קיבלה רוחב-מפורש (redistribution מדורג "גונב" את מדידת-הבסיס של העמודה הבאה) + לולאת-גובה-שורות רצה אחרי שהעמודות כבר צומצמו וגרמו לגלישת-טקסט → גובה מנופח מוכפל שוב ב-scaleY; תוצאה שנתפסה: גרירת 75px/50px ניפחה טבלה ל-7.39 ס"מ במקום ~3.90 הצפוי, עם עמודות לא-אחידות. **תוקן**: read-all-then-write-all — כל המדידות נקראות פעם אחת בתחילת `_applyTableSize`, לפני כל כתיבה. אומת חי: גדילה/כיווץ מדויקים עם חלוקה אחידה, רצפות (24px/עמודה, 18px/שורה) בלי NaN, כיווץ-עד-גלישה מרחיב שורה בפועל (פיזיקת-CSS תקינה כמו Word, לא באג), חסימת-maxTableW זהה לגרירת-קצה, ⠿ הקיימת (DOM-reorder) לא נשברה (reorder מלא נבדק). מרקוס `insert` 75/75 + `page-rules` 18/18, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-tblresize-2026-07-12.html`. פירוט: wiki/log.md "2026-07-12ב"). קודם — 2026-07-12 (**qa-sweep טבלאות — 4 תיקוני a11y/perf**: `openBorderPanel` הוסב ל-`_mwPanelDismiss` (Escape+`role=dialog`, היה pointerdown-בלבד; close נשמר כ-`_borderPanel._close` וכפתור-ההחלה/פתיחה-חוזרת סוגרים דרכו) + שמות-צבע עבריים ל-8 ה-swatches (`bdrNames` → title+aria-label, היה hex בלבד) + aria-label ל-input-הצבע-המותאם; **`openTableSizePanel` חדש** (~18441, ליד `openCellBorderPanel`; פריט "רוחב עמודה / גובה שורה" ב"עוד" של סרגל-הטבלה) — מסלול-מקלדת ל-resize מדויק בס"מ (הגרירה בגבולות היא pointer-only), אותה מכניקה כמו גרירת-קצה: `tableLayout=fixed`+colgroup+כל-תאי-העמודה, רוחב-טבלה בדלתא חסום ל-`maxTableW` (1 ס"מ מקצה-הדף §3) וממורכז ב-`marginInlineStart` שלילי, גובה=`tr.style.height` רצפת-18px; **perf** — hover-גבולות (`mousemove` col/row-resize על `#editor`, ‎2×getBoundingClientRect לאירוע) נעטף rAF-throttle (`_tblHoverEv`, דפוס `_grpMoveRAF`/`_repositionThrottled`), mousedown לא שונה. אומת חי (Escape/aria/דיוק-ס"מ/חסימת-19-ס"מ-ממורכזת) + מרקוס `insert` **75/75** ו-`page-rules` **18/18** — בייסליינים חדשים (היו 70/75 ו-15/18; ממצאי-ה-cross-page המתועדים נעלמו עם החזרת `_clamp` לעמוד-אחד 2026-07-11), 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-tblsweep-2026-07-12.html`. פירוט: wiki/log.md "2026-07-12"). קודם — 2026-07-10ה (**סבב-בדיקות-1 הושלם — 4 suites חדשים ב-QA (img-edit/ctx-tabs/calculator/export-security)**: המשך ל-"2026-07-10ב" (פערי-כיסוי 🔴 מ-FEATURES-INDEX.md) — נוסף `export-security` (4 תרחישים) ל-`qa/lib/scenarios.mjs`: `export-title-escape`/`share-title-escape`/`print-title-escape` (payload `<script>alert(1)</script> & "test"` בשם-מסמך, יירוט `URL.createObjectURL`/`window.open` כדי לתפוס את ה-HTML המיוצא בפועל — כולם בורחים נכון, 0 XSS) ו-`word-export-bom` (Blob נקרא כבתים — `arrayBuffer()` לא `.text()` — 3 בתים ראשונים `[0xEF,0xBB,0xBF]` אמיתי). **0 באגים נמצאו** — כל תיקוני-האבטחה מ-2026-07-07ג/ד (titleEsc בשלושת נתיבי-הייצוא, BOM אמיתי) עדיין תקפים בקוד החי. **סבב-בדיקות-1 סגור במלואו** (עם `img-edit` 4 + `ctx-tabs` 6 + `calculator` 5 שנוספו קודם ב-"2026-07-10ב" = 19 תרחישים חדשים סה"כ). מרקוס `insert` 70/75 + `print` 10/10 — זהים לבייסליין, 0 שגיאות console. פירוט: wiki/log.md "2026-07-10ב"). קודם — 2026-07-10ד (**סיום לולאת-שיפורי-צורות ברמת קנבה — סבב 3 מתוך 3 (תוכן+זרימת-עבודה)**: **(3.1) טקסט בתוך צורה** — דאבל-קליק על `.free-shape-wrap` (מחווט ב-`insertFreeShape` וב-סניף-`shape` של `_deserializeFreeObjs`) קורא ל-`_shapeDblClickToText`→`_ensureShapeTextLabel` (~10440-10475): יוצר `div.shape-text-label[contenteditable]` **כשכן** של `.free-shape-wrap` (לא צאצא!) — קריטי כי `_renderShape` מנקה `wrap.textContent=''` בכל רינדור-מחדש (שינוי מילוי/קו/רדיוס), ותווית-שכן שורדת את זה בעוד תווית-צאצא הייתה נמחקת בכל שינוי-צבע. CSS `inset:18%` (סוף ה-`<style>`) ממרכז את הטקסט בלי לגעת בקצוות צורות לא-מלבניות (לב/כוכב וכו'). מסונכרן ל-`dataset.shapeText` ב-`input` (לא נקרא מה-DOM ב-serialize — נשאר עקבי גם ללא-פוקוס); ריקון-בלור מסיר את התווית + מנקה dataset (לא משאיר overlay ריק חוסם-קליקים). עיצוב-טקסט (מודגש/צבע/גודל) עובד "בחינם" דרך כפתורי-הבית הקיימים (execCommand פועל על מיקוד, לא דורש UI ייעודי) — תואם למגבלה שתוארה בתוכנית. **(3.2) העתק/הדבק סגנון (Format Painter לצורות)** — `_captureShapeStyle`/`_applyShapeStyle` (~10988-11010, ליד שאר פונקציות-האפקטים): לוכד/מחיל fill+fillGradient+stroke+strokeW+dashStyle+shadow+cornerRadius+opacity (**לא** צורה/גודל/מיקום/סיבוב) בבת-אחת; `_applyShapeStyle` קורא ל-`_renderShape` בסוף שכבר מפעיל `_applyShapeFx` מבפנים (צל/גרדיאנט חוזרים לבד, אין קריאה כפולה). כפתורי 🖌️/🎯 בריבון shapeformat (קבוצת "סגנון צורה") + ב-ctxBar-צורה ("עוד") + כפתור-בונוס "🎯 הדבק סגנון לכולן" ב-`buildMulti` (מחיל על כל חברי-הבחירה-המרובה שהם צורות — נוח לעצב-אחת-ולהדביק-על-כמה, לא נדרש מפורש בתוכנית אך זול/נמוך-סיכון). אומת חי: תווית שורדת שינוי-fill (בדיקה ישירה: `_renderShape` אחרי שינוי צבע — התווית והטקסט שלה נשארים), round-trip serialize→deserialize משחזר טקסט+נראה-מיד-בלי-דאבל-קליק-נוסף, ריקון מסיר נקי; copy-style מעתיק את **כל** 7 המאפיינים בין מלבן-מעוצב לאליפסה-ברירת-מחדל ומאומת ברמת ה-SVG בפועל (fill/stroke/stroke-width/dasharray/wrapFilter). מרקוס `insert` 70/75 + `free-objects` 12/14 (אותם ממצאי cross-page מתועדים) + syntax-check 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-shapes-round3-2026-07-10.html`. **סיכום 3 הסבבים (המטרה: "כל היכולות של Canva לצורות + שליטה ברמת-תפריט דומה"):** סבב 1 (סגנון) — גרדיאנט לינארי+צל-מתכוונן+היפוך התגלו **כבר קיימים** בקוד לא-מתועד ("הרחבת-צורה שלב 3" 2026-07-08ב, נחשף בעת התכנון) ואומתו-בלבד; נוסף גרדיאנט **רדיאלי** + **רדיוס-פינה**. סבב 2 (טרנספורם/פריסה) — מיקום/גודל/זווית מדויקים (X/Y/W/H/° בס"מ), יישור-לעמוד 6-כיוונים (עודכן להחליף "מרכז אופקית" הבודד), פיזור+יישור-מתקדם לבחירה-מרובה, **קיבוץ-קבוע** (`dataset.groupId` משותף + הרחבה-אוטומטית-לקליק במקום wrapper-DOM חדש — סיכון-מינימלי, משתמש-חוזר בכל תשתית ה-multi-select הקיימת ללא נגיעה ב-drag/resize/rotate math). סבב 3 (תוכן) — טקסט-בתוך-צורה (תווית-שכן ל-wrap, לא-נמחקת ברינדור-מחדש) + Format Painter לצורות. **החלטה מוצהרת שלא-מומשה בכוונה:** מיזוג-בוליאני נשאר הרסני (הופך לתמונה שטוחה) — שינוי ארכיטקטוני נפרד, לא נענה ע"י המשתמש איזה כיוון מועדף; ושינוי-גודל/סיבוב עדיין פר-אובייקט בקבוצה (לא bbox-קבוצתי מלא כמו Canva). אפס רגרסיה לאורך כל 3 הסבבים (מרקוס `insert`/`free-objects` זהים לבייסליין בכל סבב, 0 שגיאות console). פירוט מלא: wiki/log.md). קודם — 2026-07-10ג (**לולאת-שיפורי-צורות ברמת קנבה — סבב 2 מתוך 3 (טרנספורם/פריסה/קיבוץ)**: המשך ישיר לסבב 1. **היפוך אופקי/אנכי (2.1)** נמצא **כבר מיושם** (אותו "הרחבת-צורה שלב 3" 2026-07-08ב) — אומת חי בלבד, לא נבנה מחדש. **(2.2) מיקום/גודל/זווית מדויקים** — `openShapeTransformPanel` חדש (~10852): 5 שדות-מספר (X/Y/רוחב/גובה ב-ס"מ דרך `getPxPerCm()`, זווית במעלות) הממופים ישירות ל-`left/top/width/height/dataset.rotation`; כל שינוי מפעיל `_clamp`+`scheduleSave`+`updatePagination`. **(2.3) יישור-לעמוד 6-כיוונים + פיזור** — `_alignObjToPage(obj,mode)` חדש (ליד `_clamp`, ~9029): אופקי (ימין/מרכז/שמאל) יחסית לרוחב הדף המלא (כמו "מרכז אופקית בדף" הישן שהוחלף בו), אנכי (למעלה/מרכז/למטה) יחסית **לעמוד הבודד** שמרכז-האובייקט נמצא בו כרגע (`pageIdx = floor(top/cycle)`) — לא לכל המסמך, כדי שיישור-אנכי יהיה שימושי במסמך רב-עמודים; מחליף את כפתור ה"מרכז אופקית בדף" היחיד ב-ctxBar-"עוד" (6 פריטים) + קבוצת-ריבון חדשה "יישור בעמוד" ב-shapeformat. לבחירה-מרובה: `_alignMultiSel`/`_distributeMultiSel`/`_openMultiAlignPanel` חדשים (יחסית ל-bbox של הבחירה, פיזור דורש 3+ אובייקטים) מאחורי כפתור "⊞ יישור מתקדם" חדש ב-`buildMulti` (לא מציף את השורה הקיימת). **(2.4) קיבוץ-קבוע (Group/Ungroup)** — העיצוב שנבחר (סיכון-מינימלי, לא wrapper-DOM חדש): `dataset.groupId` משותף על כל חברי-הקבוצה (במקום `_multiSel` החולף) + `window._enterGroupMulti(groupId)` (נחשף מה-IIFE של `buildMulti`) המורחב אוטומטית לבחירה-מרובה-של-כל-החברים כשלוחצים על כל חבר — ממומש דרך **עטיפה שלישית על `_selectObj`** (אחרי עטיפת `initCtxFormatTabs`, ~19570): אם לאובייקט-הנלחץ יש `groupId` תקף (2+ חברים) מרחיבים ל-multi-select ומדלגים על הבחירה-הבודדת הרגילה; אחרת נופלים חזרה לשרשרת-הבסיס ללא שינוי. משתמש-חוזר בכל תשתית ה-`_multiSel` הקיימת (גרירה-קבוצתית/`_grpMoveRAF`, נעילה, מחיקה, ריבוד) — **לא** נכתב מחדש drag/resize/rotate math. כפתורי 🔗קבץ/🔓פרק ב-`buildMulti` (מותנים ב-`_multiSel.size>=2`/יש-groupId-בבחירה). שדה `groupId` נוסף ל-`_serializeObj` + לכל 5 סניפי `_deserializeFreeObjs` (image/textbox/note/shape/drawing, ליד `if(d.locked)` המשותף). **היקף מוצהר:** שינוי-גודל/סיבוב עדיין פר-אובייקט (לא bbox-קבוצתי כמו Canva) — decision מתועדת, ניתן להרחיב בסבב עתידי אם יתבקש. אומת חי: transform-panel (X=5cm/Y=3cm/W=4cm/H=2cm/angle=30° מדויקים), align-6-כיוונים (right/center-h/left/top/middle-v/bottom — כל הנוסחאות אומתו ידנית מול page.offsetWidth/Height), distribute (מרווח שווה מחושב+מאומת), group→auto-expand-בקליק-בודד (חבר-קבוצה מרחיב, אובייקט-לא-בקבוצה לא נכלל), round-trip serialize→deserialize משמר groupId, ungroup מנקה. מרקוס `insert` 70/75 + `free-objects` 12/14 (אותם 5+3 ממצאי-cross-page מתועדים) + syntax-check 0 שגיאות console — הכל זהה לבייסליין. גיבוי `backups/MyWord-v2.backup-shapes-round2-2026-07-10.html`. פירוט: wiki/log.md). קודם — 2026-07-10ב (**לולאת-שיפורי-צורות ברמת קנבה — סבב 1 מתוך 3 (סגנון+אפקטים)**: מטרת-משתמש — להשוות את יכולות הצורות ל-Canva + שליטה ברמת-תפריט דומה. בבדיקת-הקוד לפני העבודה התברר ש**גרדיאנט-לינארי/צל-מתכוונן/היפוך כבר מומשו ומתועדים** (ר' רשומת "הרחבת-צורה שלב 3" 2026-07-08ב למטה) — לא נבנו מחדש, רק אומתו חי מול הקוד הקיים. התוספת האמיתית של הסבב: **(1) גרדיאנט רדיאלי** — `openShapeGradientPanel` קיבל בורר-סוג (`<select>` לינארי/רדיאלי, `g.type`); `_applyShapeFx` (~10610) מפצל בין `<linearGradient>` (כמו קודם) ל-`<radialGradient cx=.5 cy=.5 r=.5>` חדש; שדה `g.type` עובר אוטומטית עם ה-JSON הקיים של `dataset.grad`/`shapeGrad` (אין צורך בשדה-סריאליזציה נפרד). **(2) רדיוס-פינה למלבן** — `dataset.cornerRadius` חדש (0-48px), `_buildShapeSVG` מקבל פרמטר `cornerRadius` ומאפשר לו לדרוס rx/ry **רק** על `rect`/`rounded` (לא נוגע ב-diagTerminal/diagDatabase שמשתמשים ב-rx/ry לצורה גיאומטרית אחרת); `openShapeCornerPanel` חדש (מחוון יחיד, gate על סוג-הצורה — מציג `showHint` אם הצורה אינה מלבן); שדה `cornerRadius` נוסף ל-`_serializeObj`/`_deserializeFreeObjs`. חיווט: שני הכפתורים החדשים בריבון `shapeformat` (קבוצת "אפקטים") + ב"עוד" של ctxBar-צורה. אומת חי: רדיאלי (`radialGradient cx/cy/r=0.5`, fill=`url(#mwgrad)`) ולינארי זה-לצד-זה, רדיוס-פינה (rx=30 בפועל, round-trip serialize→deserialize משמר, ellipse מקבל hint ולא נפגע), מרקוס `insert` 70/75 + `free-objects` 12/14 — זהים לבייסליין (אותם 5+3 ממצאי-cross-page מתועדים, לא רגרסיה), 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-shapes-round1-2026-07-10.html`. פירוט: wiki/log.md). קודם — 2026-07-10 (**סנכרון-ידע מרקוס+דאן**: `docs/FEATURES-INDEX.md` חדש — אינוונטר ~120 יכולות לפי 12 אזורים, עוגן-קוד+התנהגות-צפויה+כיסוי-בדיקה לכל אחת, כולל סעיף "פערי-כיסוי" מדורג-סיכון (11 פריטים, ~30 יכולות בלי תרחיש-מרקוס — הכי בולט: פאנלי עריכת-תמונה/לשוניות-הקשר/מחשבון). 3 SPEC חדשים: `SPEC-IMAGE-EDIT.md`/`SPEC-CTX-TABS.md`/`SPEC-CALC.md`. עודכנו `spec-expert.md`(דאן, FEATURES-INDEX ראשון ברשימת-הקריאה)+`marcus-word.md`(המפקד)+`marcus-insert.md`+`CODE-INDEX.md` (פוינטרים). פירוט: wiki/log.md). קודם — 2026-07-08ב (**הרחבת-צורה שלב 3 — צל/גרדיאנט/היפוך בסגנון קנבה**: `_applyShapeFx` (~10593, drop-shadow על `.free-shape-wrap` + `scale()` להיפוך + `<linearGradient>` עם id-טרי בכל רינדור) + `_shapeFlip` + `openShapeShadowPanel`/`openShapeGradientPanel` (mw-panel, דפוס `openStrokePanel`). חיווט: קבוצת "אפקטים" חדשה בריבון `shapeformat` (4 כפתורים) + 4 פריטים ב"עוד" של הסרגל הצף. **★★ שני פערים קריטיים נמצאו ותוקנו ע"י הסשן הראשי** (ריצת-הבנייה נעצרה פעמיים עקב מגבלת-סשן, לפני חיווט-UI ולפני תיקון-סריאליזציה): (1) `_deserializeFreeObjs` מעולם לא שיחזר `shapeShadow`/`shapeGrad`/`flipH`/`flipV` מ-`obj.dataset` — אפקטים נעלמו בשקט אחרי כל שמירה+רענון; נוספו 4 שורות `if (d.*)` לפני `_renderShape(obj)`. (2) הפאנלים/`_shapeFlip` היו קיימים כפונקציות `window.*` אך **בלי שום כפתור בממשק** שקורא להן — נוספה קבוצת-הריבון + פריטי-"עוד". גם תוקן: שכפול (Ctrl+D/כפתור) של צורה-עם-גרדיאנט שכפל את `<linearGradient id>` הפנימי → 2 SVG עם id זהה בדוקומנט; נוסף `_applyShapeFx(clone)` אחרי clone לרענון-id. אומת חי: round-trip serialize→deserialize משמר צל+גרדיאנט+flip; שכפול מייצר ids ייחודיים (mwgrad3/mwgrad4); מרקוס insert 70/75 + free-objects 12/14 זהים לבייסליין, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-imgedit-phase3-2026-07-08.html`. פירוט: wiki/log.md). קודם — 2026-07-08 (**הרחבת-תמונה/צורה שלב 2 — לשוניות-הקשר בריבון**: `initCtxFormatTabs` (~18845-19057) — "🖼️ עיצוב תמונה" (`imgformat`) + "🔷 עיצוב צורה" (`shapeformat`), מופיעות רק כשאובייקט מתאים נבחר (עטיפת `_selectObj`, לא-קופצות-אוטומטית כמו Word, ביטול-בחירה מחזיר ל"בית"); נוצרות דינמית ב-JS בכוונה כדי לא להיקלט ב-`_allTabs`/מאזין-הקליק-הסטטי — אפס השפעה על 6 הלשוניות הקבועות; ריבון-תמונה: מסננים/הסר-רקע/alt/מסגרת/שקיפות/גלישת-טקסט(IMG_MODES)/ריבוד/שחזר-מקורי; ריבון-צורה: מילוי/צבע-קו/עובי/מיזוג×4/ריבוד/שכפל/מחק — הכל דרך פונקציות קיימות בלבד. אומת חי (מחזור-מלא תמונה+צורה, רק ribbon אחד active) + מרקוס insert 70/75, free-objects 12/14, home-ribbon 46/46 — זהים לבייסליין, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-imgedit-phase2-2026-07-08.html`. פירוט: wiki/log.md). קודם — 2026-07-07ז (**הרחבת-תמונה שלב 1 — עריכת-תמונה מקצועית לכל תמונה בעורך**: 3 פאנלים חדשים (~18378-18760, CSS ~3642): `openImageFiltersPanel` — בהירות/ניגודיות/רוויה 0-200% עם preview-חי ו-bake-לפיקסלים דרך canvas (שורד סריאליזציה §6); `openBgRemovePanel` — הסרת-רקע chroma-key (קליק-על-צבע + מחוון-סבילות 0-100) + מברשת ידנית מחק/שחזר עם מחוון-גודל 4-80px; `openImageBorderPanel` — מסגרת+פינות-מעוגלות (CSS על `.free-img-wrap`, לא bake). `_imgEnsureOrig` שומר `dataset.origSrc` פעם-אחת → "שחזר תמונה מקורית" תמיד זמין; `_serializeObj`/`_deserialize` שומרים/מחזירים `imgOrigSrc`. חיווט: 🌗+✂️ בסרגל-האובייקט (2 אתרים) + 3 פריטים ב"עוד" של הסרגל הצף (חוק 2-השורות §8 נשמר). אומת חי: chroma מדויק (רקע-אדום alpha=0, אובייקט-כחול 255), bake משנה src ל-PNG בפועל, מרקוס insert 70/75 + free-objects 12/14 זהים לבייסליין, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-imgedit-phase1-2026-07-07.html`. **הערת-תהליך:** הסוכן-הבונה נתקע אחרי סיום-הקוד ולפני התיעוד — הרשומה הושלמה ע"י הסשן הראשי אחרי אימות מלא מול הקוד החי. פירוט: wiki/log.md). קודם — 2026-07-07ו (**סיום לולאת-השיפורים המורחבת — סבב 3 מתוך 3 (מחשבון/פריסה/תצוגה/ייצוא/קובץ)**: ★ תוקן XSS אמיתי ב-`openSnippetsPanel()` (שם-קטע `s.name` הוזרק ל-innerHTML בלי בריחה — תוקן עם `esc()` מקומי, אותו דפוס כמו שאר הפנלים); נגישות-מקלדת לשורות-היסטוריית-מחשבון (`#_calcHist .ch-r` קיבלו `role=button`+`tabindex=0`+`aria-label`+מאזין Enter/Space); ★ תוקנו 2 מקרי mojibake תפקודיים במחשבון — חץ שבור `ג†’`→`→` ברשימת-צורות-גיאומטריה (**היה גם delimiter פונקציונלי שנכשל בשקט** ב-`.split()` לבניית טבלת-ריכוז), ו-`xג‚‚`→`x₂` בפתרון-משוואה-ריבועית. פריסה/תצוגה/ייצוא/קובץ נמצאו מוצים (ללא שינוי, defense-in-depth דרך `_sanitizeStored` מכסה sanitize-תוכן בייצוא). אומת: syntax 3/3, Playwright חי, מרקוס על 9 סוויטות (`insert` 70/75, `layout-ribbon` 6/6, `view-ribbon` 5/5, `print` 10/10, `page-rules` 15/18, `home-ribbon` 46/46, `tools-ribbon` 6/6, `keyboard` 20/20, `free-objects` 12/14) — זהות לבייסליין, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-featloop2-round3-2026-07-07.html`. **סיכום 3 הסבבים:** נגישות שיטתית (role=dialog/tablist/tab, aria-selected/pressed/label) על מחשבון (9 מצבים + לשוניות-משנה דינמיות)/פריסה/תצוגה/מנהל-מסמכים; 5 תיקוני-אבטחה אמיתיים (BOM שבור בייצוא-Word, HTML-injection במאפייני-מסמך, 3 וקטורי בריחת-HTML בייצוא/שיתוף, XSS בקטעים-שמורים); 3 מקרי mojibake תפקודיים (כיוון-טקסט, חץ-רשימת-צורות עם delimiter שבור, x₂ במשוואה). אפס רגרסיה לאורך כל 3 הסבבים. פירוט: wiki/log.md). קודם — 2026-07-07ה (**המשך-סבב 2 — השלמת לשוניות-משנה במחשבון שנטענו-בטעות כ"כבר בוצע" ברשומת "ד"**: `role=tablist/tab`+`aria-selected` על `#_eqType*` (7 סוגי-משוואה), `#_trigModeCos/Sin`+`#_trigSubModes` (תת-מצבי-טריג׳ דינמיים), `#_eqPhysicsMenu` (9 נוסחאות-פיזיקה דינמיות); `aria-pressed` על `#_trigAngleBtn`+`#_calcAngle` (2 מתגי DEG/RAD נפרדים); `role=radiogroup` ל-`#_trigInsertOpts`; `label for="_eqPctSub"` (היה לא-מקושר); `role=img`+`aria-label` דינמי ל-`#_geoSVG` (שרטוט-גיאומטריה, היה בלי alt-מקביל). שאר 4 האזורים (פריסה/תצוגה/ייצוא/קובץ) אומתו כממוצים — אין שינוי קוד. אומת: syntax 3/3, Playwright חי, מרקוס על 5 סוויטות (`insert` 70/75, `layout-ribbon` 6/6, `view-ribbon` 5/5, `print` 10/10, `page-rules` 15/18) — זהות לבייסליין, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-featloop2-round2-2026-07-07.html`. פירוט: wiki/log.md). קודם — 2026-07-07ד (**לולאת-שיפורים חדשה — סבב 2 מתוך 3 (מחשבון/פריסה/תצוגה/ייצוא/קובץ)** — add-only, המשך ישיר לסבב 1. **מחשבון**: `aria-label` ל-14 שדות-קלט/סלקט ללא-תווית ב-4 פאנלים (יחידות/סטטיסטיקה/משתנים/גרף — `#_geoShape`,`#_unitCat/Val/From/To/Swap`,`#_statInput`,`#_varName/Value`,`#_graphFn/XMin/XMax/YMin/YMax`) + `aria-label` ל-3 כפתורי-מקלדת עמומים (⌫/±/⊙). **פריסה**: `aria-label` ל-`#loBgCustom` (input color). **תצוגה**: נבדק ונמצא ממוצה (7/7 toggle כבר aria-pressed מסבב 1). **ייצוא/הדפסה**: ★ תוקן וקטור-בריחת-HTML אמיתי שנותר פתוח ב-`exportAndPrint()` (לא תוקן בכלל בסבב 1) + ★ הושלמה בריחת-HTML ב-`shareDoc()` (2 מקומות נוספים — share-banner + טקסט-פופאפ — עדיין השתמשו ב-title הגולמי למרות ש-titleEsc כבר חושב). **קובץ**: ★ `role=dialog`+`aria-modal`+`aria-label` על מנהל-המסמכים (`openDocManager`, היה חסר תפקיד-נגישות לגמרי). תגלית מחוץ-להיקף (מדווחת, לא תוקנה): XSS דומה ב-`openSnippetsPanel` (לשונית כלים). אומת: syntax 3/3, Playwright חי לכל 5 האזורים, מרקוס על 9 סוויטות (זהות לבייסליין, 0 שגיאות console). גיבוי `backups/MyWord-v2.backup-featloop2-round2-2026-07-07.html`. פירוט: wiki/log.md). קודם — 2026-07-07ג (**לולאת-שיפורים חדשה — סבב 1 מתוך 3 (מחשבון/פריסה/תצוגה/ייצוא/קובץ)** — add-only, 5 אזורים חדשים (נפרד מלולאת טבלה/תמונה/צורה/תיבת-טקסט שהושלמה). **מחשבון** (`openCalculator`): `role=dialog`+`aria-label` על `#_calc`, `role=tablist/tab`+`aria-selected` מסונכרן ל-9 כפתורי-מצב, Escape-לסגירה (self-removing כמו §8), `alt` דינמי לתמונת-גרף שמוכנסת למסמך + `aria-label` דינמי ל-`#_graphCanvas`. **פריסה**: תוקן mojibake אמיתי ב-`#loDirLtr` (`ג‡¥`→`⇥`), `aria-pressed` ל-`loDirAuto` (כיוון-חכם), `aria-label` לסלקטי רווח-פסקה. **תצוגה**: `aria-pressed` ל-7 כפתורי-toggle (ערכות-נושא×4, דף-כהה, מיקוד, שורת-סטטוס). **ייצוא**: ★ תוקן BOM-שבור אמיתי בייצוא-Word (`ן»¿`→`﻿` תו-BOM אמיתי, `exportDocWord`) + ★ נוספה בריחת-HTML ל-`<title>` בייצוא-HTML/שיתוף (`exportHTML`/`shareDoc`, לא היתה קודם — שם-מסמך עם `<>&` שבר את ה-HTML המיוצא). **קובץ**: ★ תוקן HTML-injection אמיתי בפנל-מאפייני-מסמך (`openDocProperties` הזריק `docTitle.value` ל-innerHTML בלי בריחה) + `role=dialog`+Escape + `aria-pressed` לשמירה-אוטומטית. אומת: syntax 3/3, Playwright חי לכל 5 האזורים, מרקוס על 8 סוויטות (`insert` 70/75, `layout-ribbon` 6/6, `view-ribbon` 5/5, `print` 10/10, `page-rules` 15/18, `home-ribbon` 46/46, `tools-ribbon` 6/6, `keyboard` 20/20, `free-objects` 12/14) — **כולן זהות לבייסליין**, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-featloop2-round1-2026-07-07.html`. פירוט: wiki/log.md). קודם — 2026-07-07ב (**סבב 3 מתוך 3 — סיום לולאת שיפורי-הפיצ'רים** — טבלאות/תמונות/צורות/תיבת-טקסט: **מפת-שמות בעברית לצורות** — `SHAPE_LABELS`+`_shapeLabel()` חדשים (~10099-10123, מקור: אותן תוויות מ-`openShapePicker`'s `cats`) מחליפים את ה-fallback הגנרי בשם-הסוג הגולמי ב-`_renderShape`'s aria-label ("צורה: לב" במקום "צורה: heart") — סוגר את הפריט שנדחה בכוונה בסבב 1; **aria-live לסטטוס-שמירה** — `#saveState` קיבל `role="status"`+`aria-live="polite"` כך ש-"⋯ מקליד"/"✓ נשמר"/"✗ אין מקום" מוכרז אוטומטית לקורא-מסך; **נגישות צ'יפי-בחירת-שורה/עמודה בטבלה** — `.tbl-chip` (מקור `~12389/12407`, לא עבר קודם דרך helper משותף) קיבל `aria-label` מפורש + `aria-pressed` שמשקף מצב-בחירה (toggle תקני), ו-`#tblSelBar` קיבל `role=group`+`aria-label` (כמו `openTablePicker` מסבב 1); **perf — `reposition()` (ctxBar/table-toolbar)** — מאזיני `scroll`(capture)/`resize` שקראו `reposition()` (כולל getBoundingClientRect) ישירות בכל אירוע גולמי נעטפו ב-rAF-throttle (`_repositionThrottled`, אותו דפוס בדיוק כמו `_grpMoveRAF` מסבב 2) — עדכון-מיקום מוגבל לפריים אחד, ללא שינוי התנהגות. **טבלה/תמונה כמעט-מוצו** — לא נמצא עוד שיפור add-only משמעותי מעבר לאלה (כל הפערים המתועדים מסבבים 1-2 כבר נסגרו). אומת: syntax 3/3 (708337 תווים), live-Playwright (shapeLabel="צורה: לב", saveLive="polite", chip aria-pressed false→true), מרקוס `--suite insert` 70/75, `--suite page-rules` 15/18, `--suite free-objects` 12/14, `--suite home-ribbon` 46/46 — **כולם זהים לבייסליין**, 0 שגיאות console. גיבוי `backups/MyWord-v2.backup-featloop-round3-2026-07-07.html`. **סיכום 3 הסבבים:** נגישות (aria-label/role/aria-live/aria-pressed) נפרשה על עשרות כפתורי-אייקון וידיות בכל 4 הפיצ'רים (טבלאות/תמונות/צורות/תיבת-טקסט) דרך helpers משותפים (`mkBtn`/`btn`/`_shapeLabel`), נגישות-מקלדת מלאה לידיות resize/rotate/free-move/table-move, alt-text לתמונות ניתן-לעריכה, תיקון mojibake בכפתורי-ריבוד, ולידציית-צבע ל-SVG-fallback (אבטחה), ו-3 שיפורי-perf ב-rAF-throttling (גרירה-קבוצתית, reposition) — כולם add-only, אפס רגרסיה בכל 4 סוויטות-מרקוס לאורך כל הלולאה. פירוט מלא: wiki/log.md). קודם — 2026-07-07 (השלמת סבב-2 מתוך 3 — 3 פריטי-ponytail הפתוחים: a11y-מקלדת לידיות resize/rotate/free-move (`_addHandles`) ולידית-טבלה (`.tbl-move-handle`, ArrowUp/ArrowDown) התגלו **כבר ממומשים** בקוד (רשומת "לא בוצע" הקודמת הושוותה נגד backup ישן-מדי); **perf** — לולאת גרירה-קבוצתית (multi-select, ~13042) הייתה מאזין `mousemove` לא-מבוקר שקרא offsetLeft/Top על כל אירוע גולמי; נעטפה ב-rAF (`_grpMoveRAF`) עם flush סינכרוני ב-mouseup לפני `_clamp` — `_clamp` עצמו לא שונה, אין שינוי-תדירות-קריאה. אומת: syntax 3/3, Playwright group-drag ייעודי (deltasMatch:true, 0 שגיאות), מרקוס `--suite insert` 70/75, `--suite page-rules` 15/18, `--suite free-objects` 12/14 — כולם זהים לבייסליין. גיבוי `backups/MyWord-v2.backup-featloop-round2b-2026-07-07.html`. פירוט: wiki/log.md). קודם — 2026-07-06ד (TTS — הוסר קריין-מובנה (`prefs.synthMode` + מודול `const _SYNTH` formant-synth ~230 שורות) לגמרי לבקשת המשתמש; נשארו 3 מסלולי-קול (Azure/שרת-מקומי/קול-מערכת); מחווני קצב-גובה תמיד גלויים ללא תנאי. פירוט: wiki/log.md). קודם — 2026-07-06 (TTS — הוסר לגמרי קריין-מותאם-אישית ("קריין שלי": שם/קול-נבחר/קצב-נפרד/גובה-נפרד/Azure-מותאם/קובץ-קלונינג) — נשארו 2 קריינים בלבד (בן/בת), `PERSONA_ORDER`; ★★ תוקן באג-אמת — מחווני קצב/גובה בפנל-הקריין (`initSpeak`) לא השפיעו בפועל על הקול במסלולי Azure (SSML חסר `<prosody>`) ושרת-קול-מקומי (`audio.playbackRate` בצד-לקוח, שיטה פגומה); תוקן לשליחת `rate`/`pitch` native לכל שרת, אומת חי (יירוט-fetch + גודל-WAV שונה בפועל בין קצבים). נוסף מסלול `prefs.xttsUrl` (שרת Python מקומי חיצוני, `ttslab/edge_server.py` עוטף edge-tts — קולות Neural חינמיים, פורט 8021) בין Azure ל-Web Speech, עם נפילה-אוטומטית. פירוט: wiki/log.md). קודם — 2026-07-06ג (סבב-1 מתוך 3 — שיפורי-פיצ'רים אדיטיביים לטבלאות/תמונות/צורות/תיבת-טקסט, "מה שעובד לא לגעת": **נגישות משותפת** — `mkBtn`/`btn` (helpers משותפים ל-_addToolbar ולכל ctxBar טבלה/תמונה/צורה/תיבה) מוסיפים `aria-label` מ-`title` אוטומטית (עשרות כפתורי-אייקון קיבלו שם-נגיש בלי לגעת בכל כפתור בנפרד); **תמונה** — כפתור חדש "🏷️ טקסט חלופי" (`_editImgAlt`, ~9540) לעריכת `alt` אחרי ההכנסה (בסרגל-האובייקט + ב"עוד" של ctxBar, לא בשורה הראשית — שומר על חוק 2-השורות §8); **תיבת-טקסט** — `role=textbox`+`aria-multiline`+`aria-label` ל-`.tb-content`, `aria-label` לידית-הגרירה (`insertFreeTextbox` ~9646, גם ל-note שמשתמשת באותה פונקציה); **צורה** — `role=img`+`aria-label` ל-`.free-shape-wrap` (`_renderShape`); **טבלה** — `scope="col"` לתאי `<th>` שנוצרים ע"י `insertTable`, בורר-הגודל (`openTablePicker`) קיבל ניווט-מקלדת מלא (tabindex+role=button+Enter/Space) ו-`role=group` לרשת; **תיקון mojibake אמיתי** — כפתורי ריבוד `_addToolbar` (הבא-קדימה/שלח-אחורה) הציגו גליפים שבורים `ג¬†`/`ג¬‡` (double-encoding, כמו הבאג התיעוד של המחשבון) — הוחלפו ל-🔼/🔽 (אומת מול קוד קיים ב-openObjMenu ~18085); **חידוד קוד** — `insertFreeImage`/`insertFreeTextbox` חדלו לשכפל את חישוב ה-z-index הידני ועברו ל-`_nextZ()` הקיים (כבר בשימוש ב-insertFreeShape), ללא שינוי התנהגות. כל השינויים אומתו: מרקוס `--suite insert` 70/75 ו-`--suite page-rules` 15/18 ו-`--suite free-objects` 12/14 — **זהה בדיוק** למספרי-הבסיס לפני הסבב (5+4+3 הממצאים הם by-design מתועד, §5 מעבר-חופשי-בין-דפים — לא רגרסיה), 0 שגיאות console בכל הרצה; גיבוי `backups/MyWord-v2.backup-featloop-round1-2026-07-06.html`. קודם — 2026-07-03b (ערכת "לילה-קרח" + סרגל-צד — תמונת-השראה של המשתמש, opt-in לא-דיפולט: `body[data-theme="night"]` + כפתור ❄️ `#vwNight` בתצוגה (אותו `myword2_theme`); טוקנים קרח (`--primary:#6FA8E8`) + דריסת `--glass-*` → אריחי Glass v2 מוסבים אוטומטית; קישוט הילות+כוכבים על `.editor-area` (חייב `!important` — "הרקע החם" ~2563 גם `!important`); הדף נשאר לבן (§view). סרגל-צד `#mwSidebar` fixed z-900: לוגו-SVG מוטמע, פעולות-מהירות→מסלולים-קיימים, מתאר-מסמך חי (h1-h3, MutationObserver 500ms), 5 אחרונים (`getAllDocs`), ⇄ צד + ✕ קיפול ל-FAB; מפתחות `myword2_sidebar_side/open`; דיפולט פתוח-ב-night; נסתר ב-print/מיקוד/<1150px. גיבוי: `backups/MyWord-v2.backup-nightice-2026-07-03.html`. פירוט: wiki/log.md). קודם — 2026-07-03 (תוקן "מקפיץ לתחילת עמוד": `editor.focus` עטוף `preventScroll:true` בנקודה אחת לפני `buildColorPalette()` — ≈40 אתרי-קריאה לא קופצים עוד ל-scrollTop=0 כשאין בחירה; גלילה מכוונת = `scrollIntoView` בלבד. "מופיעים בתחילת עמוד": `insertFreeImage`/`insertFreeTextbox` עברו ל-`_newObjInsertPoint` (סמן חי → אחרת מרכז-האזור-הנראה) במקום `_savedRange`-ישן/60-80px קבוע — מיושר עם צורות/מחברים. פירוט: wiki/log.md). קודם — 2026-07-02b (§2 עיצוב Glass v2 — בחירת-משתמש Premium Glassmorphism, CSS-בלבד בסוף ה-style: אריחי-זכוכית לאייקוני `.ribbon-big .ic` (36px, רפלקציה, זוהר, hover-ריחוף) + יישור-גובה אחיד 60px לכל הסרגלים + caret-הרחבה בצד (`.ribbon-split` row) + טאבים בולטים (פעיל=גרדיאנט אינדיגו, לא-פעיל=זכוכית) + סרגל-בית glass + dark-variants; `buildTextbox` שורה-2 סודרה [רקע·מסגרת]|[שכפל·נעל·בחר-כמה]|[מחק] + ▾ על כפתור-המסגרת; גיבוי `backups/MyWord-v2.backup-design-2026-07-02.html`. קודם — 2026-07-02 (§5/§16 טבלה+צורות — כל רשימת-הבאגים של המשתמש נסגרה: §5 מעבר-חופשי-בין-דפים לאובייקטים חופשיים (ביטל "אובייקט שייך לדף אחד", אישור מפורש); צורות — מיזוג-Paper.js אמיתי לכל הצורות (rect/ellipse/polygon, לא רק path, expandShapes) במקום נפילה-שקטה ל-SVG-mask; בחירה-מרובה Shift/Ctrl+Click; עוגן-חיתוך כחול; ריבוד-קדימה `_fwdZ`=מעל-כולם-בלחיצה-אחת. תמונה — לחיצה פותחת סרגל-צף; מצב-inline cursor:move; float-gap מטקסט. טבלה — `_clamp` בגרירת-קצה; מיזוג/פיצול/מחק-טבלה גלויים; max-width 16cm→19cm (התנגש עם גדילה-לתוך-שוליים); קליק-קצה=בחר-שורה/עמודה תוקן (היה מת-קוד: RTL-index הפוך + IIFE scope-isolation, typeof-guard נכשל בשקט — תובנה: קוד מפוצל ל-IIFE-ים נפרדים, guard על מזהה-חוץ תמיד נכשל בלי שגיאה אם לא נחשף ל-window). קודם — 2026-06-26 (§16 qa-sweep: #1 unite/exclude אמיתי ב-Paper.js + כפתור XOR; #3 max-width טבלה 16cm; #4 אין-באג; #5 openCellBorderPanel; #6 מחיקת-סימנייה; #7 דף-כהה opt-in #vwPageDark; #2 העברת-טבלה DOM-reorder .tbl-move-handle; גלילה-אוטומטית בגרירה _edgeAutoScroll (כל אובייקט+ידית-טבלה); ידית-גרירה ⠿ .free-move-handle לכל אובייקט חופשי. קודם — 2026-06-23: §16 סרגל-צף: גלגלת-מחליפה-לשונית + מעבר-סטֵג בלי הוספה-אוטומטית + ▾ split לטבלה בסרגל הרגיל + תיקון גלילה אופקית + כותרות לכפתור-split; תמונה float אופקי 2.5→1.0cm; שתי מערכות סרגל-צף — floatbar-v04 ~14279. רישום מפורט פר-סשן: כספת אובסידיאן `wiki/log.md`. קודם — 2026-06-12c: §11 openColorWheel, §16 כיוון-חכם + קריינים; §2 מצגת הוסרה; §8 דליפת dismiss; 2026-06-11: §2 שש לשוניות, §13 F9; §4 splitContainer+cycle+clamp; §5 FREE_MARGIN_CM=1.0; §11 margin:0)._

---

## 1. ארכיטקטורה — קובץ יחיד
- המוצר הוא **HTML יחיד** — כל CSS, JS ו-HTML בקובץ אחד.
- אין תלויות חיצוניות (אין CDN, אין npm, אין iframe). **ספריות צד-ג' חייבות להיות מוטמעות בקובץ עצמו** (inline `<script>`), לא נמשכות מהרשת — כך הקובץ עובד אופליין לחלוטין.
  - **חריג מאושר (2026-06-25):** **Paper.js 0.12.17** מוטמע inline (≈235KB, ב-`<script>` סביב שורה 2850) לצורך מיזוג-צורות בוליאני אמיתי (`paperBooleanOp`, §5/§16). אושר מפורשות ע"י המשתמש. **אסור להחזירו ל-CDN ואסור למחקו** בלי אישור. אם נטען נכשל — נפילה אוטומטית ל-SVG mask ב-`combineSelectedShapes`.
- שפת ממשק: **עברית, כיוון RTL** (`dir="rtl"`).
- **הוספת קוד:** CSS חדש תמיד **בסוף** ה-`<style>`; JS חדש תמיד **לפני** `buildColorPalette();`.

---

## 2. מבנה ה-UI — Ribbon בסגנון Word
### לשוניות (Tabs)
| data-tab / data-ribbon | תוכן |
|---|---|
| `file` | סרגל קובץ — שמירה, פתיחה, **ייצוא (Word/PDF/HTML)**, שיתוף, הדפסה |
| `home` | סרגל הבית — עיצוב טקסט ופסקה |
| `insert` | סרגל הוספה — תמונות, טבלאות, צורות, קישורים, כלים |
| `layout` | סרגל פריסה — כיוון טקסט (rtl/ltr/חכם), רווח פסקה, אוטו-עיצוב |
| `tools` | סרגל כלים — איות, סטטיסטיקה+יעד, הכתבה קולית, הקראה, תקן-שפה (F9), קטעים, תוכן עניינים |
| `view` | סרגל תצוגה — ערכת נושא בהיר/כהה/**לילה-קרח** (❄️ `#vwNight`)/**ניאון-מעגלים** (`#vwNeon`) — שתי האחרונות opt-in, פותחות את `#mwSidebar`, **והדף נשאר לבן**; **רקע דף (לבן/קרם/תכלת/מותאם)**, מיקוד, שורת סטטוס, זום |

**חוק:** רק ribbon אחד מוצג בכל רגע (`display:none !important` לשאר).
**חוק:** `.ribbon.active` = `display:flex !important`.
**חוק:** הלשונית `home` היא `.ribbon.active` בטעינה.

### מבנה Ribbon
```
section.ribbon[data-ribbon="..."]
  └── div.group-titled          ← קבוצה (כמו "גופן", "פסקה")
        ├── div.rows
        │     └── div.row
        │           └── button.ribbon-big   ← כפתור גדול עם אייקון + תווית
        └── div.label            ← שם הקבוצה (10px, מתחת)
```

---

## 3. חוקי דף A4 — בלתי ניתנים לשינוי
| פרמטר | ערך |
|---|---|
| רוחב דף | `21cm` |
| גובה דף | `29.7cm` |
| שוליים (padding) | `2.5cm` מכל צד |
| פער בין דפים | `1.5cm` |
| גובה מחזור (cycle) | `29.7cm + 1.5cm = 31.2cm` |
| גופן ברירת מחדל | `14pt`, line-height `1.6` |

**חוק:** ה-`padding` של `.page` הוא השוליים. **אסור** להוסיף margin לאלמנטים בתוך העורך לשם "עימוד" — זו אחריות `paginateBlocks()` בלבד.

### מיקום על ציר Y
```
דף 1:  0       → 29.7cm
פער:   29.7cm  → 31.2cm  (אסור לכתוב כאן)
דף 2:  31.2cm  → 60.9cm
פער:   60.9cm  → 62.4cm
...
```

### CSS mask — הדפים הוירטואליים
```css
mask: repeating-linear-gradient(
  to bottom,
  black 0, black 29.7cm,
  transparent 29.7cm, transparent 31.2cm
);
```
זה יוצר מראה "דפים נפרדים" מבלי לפצל את ה-DOM. **אסור לשנות ערכים אלה.**

---

## 4. Pagination — `paginateBlocks()` + `updatePagination()`
### מה עושה `paginateBlocks()`
- סורקת את ילדי העורך הישירים.
- כל ילד שנוחת בתוך פער (29.7cm → 31.2cm, או בכפולות) — מקבל `margin-top` זמני שדוחף אותו לתחילת הדף הבא.
- **מאפסת דחיפות קודמות** לפני כל ריצה.
- מוגנת מ-re-entrant ע"י `paginateBlocks._busy`.
- **מודעת לכותרת/תחתית רצה:** כשיש `_docHeader`/`_docFooter`, משאירה `1.8cm` פנוי בראש עמודי-המשך ו-`1.6cm` בתחתית (ראה §9). בלי כותרת/תחתית — דוחפת לקצה העמוד הבא ללא תוספת.
- **פיצול מיכלים — `splitContainer()` (כמו Word):** מיכל ארוך (DIV מקונן מהדבקה) שחוצה גבול-עמוד **לא נדחף שלם** (זה משאיר חצי עמוד ריק). במקום זאת `splitContainer(cont)` דוחף את ילדיו הבודדים שחוצים את הגבול — רקורסיבית, יורד דרך עוטפי-DIV-יחיד אל רמת הפסקה/LI. `isSplittable` = DIV/OL/UL/BLOCKQUOTE; פסקה/כותרת/LI = יחידה אטומית. `pushEl(el, extra)` = פונקציית הדחיפה המשותפת. אם הילד הראשון כבר חוצה — נופלים לדחיפת המיכל השלם.
- **חלוקת-עבודה עם `_paginateLists` (קריטי!):** רשימה UL/OL שהיא **ילד ישיר** של העורך **לא** מטופלת ע"י `splitContainer` — היא נשארת ל-`_paginateLists` (ב-`paginateSplitPatch`, רץ מיד אחרי) שמפצל אותה **פיזית** ל-chunk לכל עמוד (`data-pglist`). דחיפת LI במרגינים מסתירה את החצייה מ-`_paginateLists` ומשאירה את תיבת-הרשימה חוצה עמודים (הפרת probe). `paginateBlocks` דוחף רשימה ילד-ישיר **שלמה** רק כשהפריט הראשון עצמו חוצה. רשימות **מקוננות** בתוך DIV כן מפוצלות ע"י `splitContainer` (כי `_paginateLists` רואה רק ילדים ישירים).
- **`data-pagebreak` ללא cascade:** פסקת שובר-עמוד נדחפת פעם אחת בלבד (`!data-pushed`); פסקה ריקה (גובה 0) עם `data-pagebreak` **כן** מעובדת (לא נדלגת ע"י `!h`).

### מה עושה `updatePagination()`
- קוראת ל-`paginateBlocks()`.
- מחשבת `naturalPages = ceil((scrollHeight + gap) / cycle)` — **חובה לחלק ב-`cycle` ולא ב-`pageH`**, כי `scrollHeight` כבר כולל את דחיפות ה-gap מ-`paginateBlocks` (חלוקה ב-`pageH` ניפחה את מספר העמודים). אותה נוסחה ב-`_pageCounts()` ובמטפל ה-`input`/delete.
- `pages = max(naturalPages, manualPages)` — `manualPages` נשמר עם המסמך. תיקון אוטומטי: אם `manualPages > naturalPages*2+3` מאפסים ל-naturalPages. בנוסף `loadDoc` מקלמפ `manualPages` ל-`_pageCounts().natural` בעת טעינה (מנקה עמודים ריקים שנשמרו מבאג ישן).
- מגדירה `editor.style.minHeight` בדיוק.
- מציירת overlays: `.page-number` + `.page-break-label` ב-`#pageStack` (לא ב-`#editor`).

### מתי לקרוא ל-`updatePagination()`
**חייב** לקרוא לה עם `requestAnimationFrame(updatePagination)` אחרי כל:
- שינוי תוכן (`input`, הוספה/הסרה של אלמנטים)
- הוספה/הסרה של אובייקט חופשי
- שינוי גודל גופן / שורה / כותרת
- ייבוא/טעינת מסמך

---

## 5. אובייקטים חופשיים (`free-obj`)
### עקרון מרכזי: "כמו Word"
Word פותר בעיות אלה מראש. MyWord מחקה את ההתנהגות. כל אובייקט חופשי הוא
`div.free-obj` עם `position:absolute`, `contenteditable="false"`, `z-index` דינמי
(ברירת מחדל 15, ראה חוק גרירה 7), ו-`data-rotation`.

| סוג | מזהה | תוכן פנימי |
|---|---|---|
| תמונה | `.free-obj` + `data-img-mode` | `.free-img-wrap > img` |
| תיבת טקסט | `.free-obj.free-textbox` | `.tb-header` + `.tb-content` |
| פתק/הערה | `.free-obj.free-textbox.free-note` | `.tb-header` + `.tb-content` |
| צורה (SVG) | `.free-obj` | `.free-shape-wrap > svg` |
| מחבר/חץ | `.free-obj.free-connector` | `.free-shape-wrap > svg.conn-svg` |
| ציור | `.free-obj` | `.free-draw-wrap > canvas` |

### חוקי מיקום
- **תמיד** `position: absolute` יחסית ל-`#editor`.
- ערכי `left/top` ב-px — **לא** ב-cm, לא ב-%.
- **חוק clamp:** `_clamp(obj)` נקרא אחרי כל גרירה/שינוי גודל/הדבקת אובייקט. מרווח קצה `FREE_MARGIN_CM = 1.0` (≈37.8px ב-96 DPI; `margin = FREE_MARGIN_CM * getPxPerCm()`). **מעבר חופשי בין עמודים לאובייקטים חופשיים (עודכן 2026-07-13, אישור מפורש של המשתמש — משחזר את הרעיון מ-2026-07-01 ומבטל את נעילת-העמוד מ-2026-07-11):** `_clamp` מאפשר לאובייקט לשבת בכל עמוד לאורך המסמך (לא נעול לעמוד-הכיל), כמו צורה צפה ב-Word. אנכית `top` מוגבל לגבולות-המסמך בלבד: `docTop = topMargin + halfExtra` (לא מעל ראש העמוד הראשון) עד `docBot = H - botMargin - objH - halfExtra` (לא מתחת לתחתית המסמך; `H` = `page.offsetHeight` — גובה-המסמך *הנוכחי בפועל*, לא גדל מעצמו). **הוספת-עמוד אוטומטית בגרירה (נוסף 2026-07-25, אישור-משתמש):** מכיוון ש-`docBot` תלוי במספר-העמודים הנוכחי, גרירה חיה שמנסה לרדת מתחת לעמוד-האחרון-הקיים הייתה נתקעת שם לצמיתות (אין "עוד עמוד" להזיז אליו). כמו ב-Word: `_onGlobalMove`'s ענף-הגרירה-החופשית מוסיף עמוד (`manualPages++`, אותו מנגנון כמו כפתור "➕ עמוד"/`addManualPage()`) *לפני* ההצבה/`_clamp`, כשה-`newT` הרצוי חורג מהעמוד-הקיים — כך הגרירה ממשיכה ברציפות והמסמך גדל לפי הצורך. **בכוונה מוגבל לגרירה חיה בלבד** — לא בלולאת ה-`_clamp` הכללית של `updatePagination` (שרצה על כל שינוי-תוכן/reflow), כדי לא לגרום לצמיחת-עמודים בלתי-נשלטת. **הרחקה מפס-הפער:** ה-CSS mask (§3) חותך כל מה שנוחת בפער-בין-עמודים (אחרת הצורה נראית קטומה — "יוצאת מהדף", דיווח-משתמש 2026-07-13), לכן אם ה-bbox חוצה פס-פער `_clamp` מזיז אותו מינימלית החוצה — למעלה (תחתית ה-bbox מסתיימת לפני הפער) או למטה (ראש ה-bbox בתחילת העמוד הבא), הקרוב מביניהם. כך אפשר להגיע לכל עמוד (מעבר חופשי) אך לעולם לא להיחתך. חל רק כשהאובייקט נכנס בגובה-עמוד (`spanH <= pageH - topMargin - botMargin`); אובייקט גבוה-מעמוד נשאר בגבולות-המסמך בלבד. אופקית — 1.0cm מכל קצה (ללא שינוי). **כל** הסוגים החופשיים (צורות, תיבות, תמונות free/above/behind, ציור) חוץ ממצבי-זרימה (float/inline המטופלים ב-`_clampFloat`, שנשאר נעול-לעמוד — הן מעוגנות-לטקסט). **`_clampFloat` לתמונות float-right/left (עודכן 2026-07-11 — הושלמה העקביות):** אנכית — אותו חוק בדיוק: מרווח 1.0cm מהפער, ואם התמונה לא נכנסת בעמוד — **נדחפת שלמה לתחילת העמוד הבא** (דרך `margin-top`, delta חיובי תמיד). *הגרסה הקודמת ניסתה להזיז למעלה (`bandBot-h`) — delta שלילי שה-guard `mt<0→0` בלע בדיוק במקרה הנפוץ (עוגן-נמוך עם margin≈0), כך שתמונות-גלישה חצו עמודים בפועל.* בנוסף, `_clampFloat` נקראת עכשיו **גם בזמן גרירה חיה** (בתוך ענף-ה-wrap של `_onGlobalMove`) לפי §15, לא רק ב-mouseup. תמונת **inline** מטופלת ע"י `paginateBlocks` (§4 — היא ילד-ישיר in-flow ולא מדולגת). אופקי (float): קצה חיצוני 1.0cm מקצה הדף, קצה פנימי (לכיוון מרכז הדף) **0.2cm ממרכז** (מאפשר לתמונה לכסות כמעט חצי עמוד).

### 6 מצבי תמונה — `IMG_MODES` (נשמר ב-`data-img-mode`)
> **תוקן 2026-07-11** — סעיף זה תיעד "7 מצבים" כולל `below-text` הרבה אחרי ש-`below-text` מוזג ל-`behind` בקוד החי (`_setImgMode` ממזג אותם כי הם זהים: `position:absolute; z-index:-1`). התיעוד לא עודכן יחד עם הקוד, מה שהוביל לתרחיש-בדיקה של מרקוס (`img-mode-cycle`) שנשאר תקוע על ההנחה הישנה. ראה זיכרון `img-modes-below-text-trap`.

`_setImgMode(obj, mode)` מחליף בין המצבים. הסדר הקנוני ב-`IMG_MODES`:
```js
['free', 'above-text', 'float-right', 'float-left', 'inline', 'behind']
```
| מצב | התנהגות | השפעה על טקסט |
|---|---|---|
| `free` | `position:absolute`, גרירה חופשית + snap | לא משפיעה |
| `above-text` | חופשי, `z-index` גבוה — מעל הטקסט | לא משפיעה |
| `float-right` | `float:right; position:relative` | טקסט עוטף משמאל |
| `float-left` | `float:left; position:relative` | טקסט עוטף מימין |
| `inline` | `display:block; position:relative` | דוחה טקסט למטה |
| `behind` | `position:absolute; z-index:-1` | מאחורי הטקסט (סמן מים) |

**⚠️ מלכודת:** כל קוד שמחזר/בורר מצב תמונה **חייב** לקרוא מ-`IMG_MODES` הקנוני, לא ממערך מקומי. אם מערך-מקומי כולל `'below-text'` — האינדקס קופץ ישר ל-`behind` ומדלג על שאר המצבים (float-right/float-left/inline נעשים בלתי-נגישים מהמחזור). לתאימות-לאחור עם תוכן ישן ששמר `below-text`: לנרמל `if (m==='below-text') m='behind'` לפני `indexOf`.

מעבר מהיר ל-`behind` וחזרה: כפתור 🔽/🔼 בסרגל הצף.

### כפתורי שינוי גודל + סיבוב + שקיפות
8 handles (`.resize-handle[data-dir="nw|n|ne|e|se|s|sw|w"]`) + handle סיבוב (`.rotate-handle`).
מופיעים **רק** כשהאובייקט `.selected`. שקיפות 0%–100% דרך מחוון `.ctx-opacity` בסרגל הצף.

---

## 6. שמירת/טעינת אובייקטים — `_serializeFreeObjs()` / `_deserializeFreeObjs()`
- `_serializeFreeObjs()` סורק את כל `.free-obj` ומחזיר מערך; `_serializeObj(obj)` מטפל באובייקט יחיד.
- תמונות מומרות ל-base64 ונשמרות בשדה `freeObjs` של המסמך.
- בטעינה: `_deserializeFreeObjs(arr)` מחזיר אותן לעורך (כולל `_clamp` + `_addHandles` + `_makeDraggable`).
- **חוק:** `max-height: 24.7cm` לתמונה inline בתוך דף (שוליים 2.5cm = נשאר 24.7cm).

---

## 7. שמירה — IndexedDB (+ מראה ב-localStorage)
> **תוקן 2026-07-19** — הסעיף נקרא "שמירה — localStorage" זמן רב אחרי שמקור-האמת עבר ל-IndexedDB.

| מקום | תוכן |
|---|---|
| **IndexedDB `mwDB`** (~5435) | **מקור-האמת** לכל המסמכים — `mwDB.put/getAll/del`; `saveDoc` כותב `mwDB.put(currentDocId, _d)`, `getAllDocs()` = `mwDB.getAll()` |
| `localStorage['myword2_docs']` | `STORAGE_KEY` — **מראה-גיבוי סינכרונית** הנכתבת בכל שמירה (בתוך try/catch), ומשמשת **fallback** כש-IDB לא זמין. יש מיגרציה חד-פעמית מהמפתח הישן |
| `localStorage['myword2_last']` | `LAST_OPEN_KEY` — id של המסמך האחרון שנפתח (localStorage אמיתי, לא מראה) |

> ⚠️ **ניקוי-מצב בבדיקות מחייב `mwDB.del` בלולאה** — `localStorage.clear()` לבדו **לא** מוחק מסמכים. ר' זיכרון [[test-hygiene-idb-not-localstorage]].

### סכמת מסמך יחיד
ה-id הוא ה**מפתח**, לא שדה. השדות בפועל:
```js
{
  title:       string,   // שם המסמך
  content:     string,   // editor.innerHTML (אחרי ניקוי דחיפות pagination)
  manualPages: number,   // מספר עמודים ידני (כפתור ➕)
  updated:     number,   // Date.now() של השמירה האחרונה
  freeObjs:    Array,    // אובייקטים חופשיים מסוריאליזים (תמונות/צורות/תיבות…)
  docHeader:   string,   // כותרת עליונה רצה (תבנית, ראה §9)
  docFooter:   string,   // כותרת תחתונה רצה
  pageHF:      object,   // הגדרות כותרת/תחתית (נוסף בעטיפת window.saveDoc)
  pageBg:      string    // רקע דף (§16 — נוסף בעטיפה שלישית)
}
```
> `saveDoc` הבסיסי כותב 4 שדות בלבד (title/content/manualPages/updated); כל השאר מתווספים ב**עטיפות** של `window.saveDoc`. לכן `scheduleSave()` חייב לקרוא ל-`window.saveDoc` ולא לפונקציית-הבסיס.

**שדות-אופציה בכל פריט של `freeObjs[]`** (כולם `if (d.x)`-guarded → מסמכים ישנים נטענים ללא שינוי):
`tbPadding` · `tbValign` · `tbAutofit` · `tbVertical` · `tbShadow` · `tbClip` · `linkedNextId` · `linkedPrevId` (תיבת-טקסט) · `cornerRadius` · `groupId` (צורה) · `imgOrigSrc` · `imgPreCrop` (תמונה — גיאומטריה שלפני חיתוך-הרקע, §16).
`repeatHeader`/`autofit`/`tblstyle` הם attributes על ה-`<table>` ולכן נשמרים בתוך `content`.
- שמירה אוטומטית: `scheduleSave()` = debounce של **800ms**.
- שמירה ידנית: Ctrl+S / כפתור "שמור".
- `saveStateEl` מציג: `"⋯ מקליד"` בזמן עריכה, `"✓ נשמר"` לאחר השמירה, `"✗ אין מקום"` בכשל.

---

## 8. סרגל הצף (`#ctxBar`) — לפי הקשר
מופיע בעת:
- בחירת טקסט — עיצוב (B, I, U, S, מרקר, צבע, גודל ±, יישור, קישור) + "עוד אפשרויות".
- בחירת אובייקט חופשי — גלישה, ריבוד, נעילה, מחיקה, בחירה מרובה, **שקיפות**, ולתמונה גם "מאחורי הטקסט".
- עריכת תא טבלה — הוספת/מחיקת שורות ועמודות, צבע תא, יישור.

**חוק:** `#ctxBar` מוצב `position:fixed` ולא נוגע בזרם המסמך.
**חוק:** נסגר בלחיצה מחוץ לו + Escape.
**חוק:** סרגל צף **חייב להיות בדיוק 2 שורות מקסימום** (`ctx-row1` + `ctx-row2`). כפתור ההחלפה (כפתור עגול) **נשאר במרכז בין השורות** (דרך `ctx-half-a` / מרכז / `ctx-half-b`). אם לא נכנסים בדיוק 2 שורות — להוציא כפתורים שנדירי-שימוש ל-`moreBtn` (עמוד 2), לא להוסיף שורה שלישית.
**חוק:** תפריט "עוד אפשרויות" (`#ctxMore`) נפתח **כלפי מטה**; אם אין מקום — גובה מוגבל עם גלילה (לא מתהפך מעלה).
**חוק:** פלטות הצבע/גודל מהסרגל העליון מקבלות `z-index` **מעל** `#ctxBar` (10050) כדי שלא יוסתרו.
**חוק (תפריט קליק-ימני `#rcMenu`, עודכן 2026-07-20):** התפריט המותאם מוצג רק כשיש לו **ערך מוסף** — בחירת-טקסט, תא-טבלה, או קישור. על **סמן-מכווץ בטקסט רגיל משאירים את תפריט-הדפדפן** (בלי `preventDefault`), כי הוא המסלול היחיד להצעות-איות ולהדבקה מלאת-עיצוב. אובייקט חופשי (תמונה/צורה/ציור) → אין תפריט (סרגל-צף), **אבל `.tb-content` כן מקבל** — זו עריכת-טקסט. הדגשת-פריט בניווט-מקלדת חייבת להיות `class`+`aria-activedescendant` ולא `focus()` (פוקוס על כפתור שובר את `execCommand`).

**חוק (פנלי `.mw-panel` — `_mwPanelDismiss`):** מאזיני הסגירה (pointerdown+keydown) נרשמים ב-**capture** עם `stopPropagation` על Escape (כדי ש-Escape יסגור את הפנל ולא יפעיל מצב אחר). **חובה:** המאזין בודק `panel.isConnected` ומסיר את עצמו אם הפנל כבר הוסר מה-DOM — אחרת פתיחה חוזרת (שעושה `.remove()` לפנל הקודם בלי `close()`) מדליפה מאזין-Escape גלובלי שבולע Escape לכל שאר ה-UI. ה-`setTimeout(50ms)` שמצמיד את המאזינים מדלג אם `!panel.isConnected`.

---

## 9. כותרת עליונה/תחתונה (`_docHeader` / `_docFooter`)
- מוזרקים כ-overlays (`.doc-header`, `.doc-footer`) ב-`#pageStack`.
- `position: absolute`, `inset-inline: 2.5cm` (מיושר לשוליים הצד).
- כולל מספור עמודים אוטומטי: `{עמוד}` / `{סהכ}` (וגם `{page}` / `{total}`).
- `paginateBlocks()` מודע להם: כשהם פעילים משאיר `1.8cm` פנוי בראש עמודי-המשך + `1.6cm` בתחתית, כך שטקסט לא עולה על הכותרת/תחתית.

---

## 10. זום
- `#pageStack { style.zoom: X }` — **לא** `transform:scale`. (zoom משנה פריסה/גלילה; transform לא.)
- טווח: 50%–200%, צעד 10%. פקדים בשורת הסטטוס + Ctrl+גלגלת עכבר.
- **חוק:** מתמטיקת הגרירה/שינוי-גודל מחלקת את ה-delta ב-`window._docZoom` כדי שאובייקטים יעקבו אחרי הסמן בכל זום.

---

## 11. כלי עיצוב טקסט
### גופן
- שם: `execCommand('fontName')` + `#fontNameSelect`
- גודל: **לא** `execCommand('fontSize')` — במקום זאת `applyFontSize(pt)`: עוטפת text-nodes ב-`<span style="font-size:Xpt">`.
  - בחירה בתוך **בלוק יחיד** → הכנסה ישירה דרך Range + **בחירה-מחדש** (הטקסט נשאר מסומן, אפשר ללחוץ שוב על ±, הסרגל הצף לא נעלם).
  - בחירה **רב-בלוקית** → `execCommand('insertHTML')` (נכנס למחסנית undo).
  - `readCurrentFontSizePt()` יורד אל התוכן הנבחר וקורא את הגודל האמיתי.

### הדבקה (paste)
מחטא הדבקה: שומר עיצוב בסיסי בלבד, מסיר `style`/`class`/`<script>`/`href="javascript:"`, ומדביק תמונות מהלוח כאובייקט חופשי תקין.

### צבע
- `execCommand('foreColor')` / `execCommand('backColor')` + פלטה מ-`buildColorPalette()`.
- **בורר צבע מקצועי `openColorWheel(opts)`** (גלגל גוון/רוויה + מחוון בהירות + HEX + צבעים אחרונים) — נפתח מהחץ ▾ של `#colorBtn`/`#bgColorBtn` ומרקע-דף מותאם. עוזרים: `_hsvToRgb`/`_rgbToHsv`/`_hexToRgb`/`_rgbToHex`. ראה §16 שלב 2.

### מברשת עיצוב (Format Painter)
כפתור `#btnFormatPainter`: לוכד עיצוב מטקסט נבחר ומחיל על הבחירה הבאה (חד-פעמי).

### סגנונות בלוק
`#blockStyle` select → `P | H1 | H2 | H3 | BLOCKQUOTE`

### יישור
`justifyRight | justifyCenter | justifyLeft | justifyFull`

### רשימות
`insertOrderedList` / `insertUnorderedList`
+ זיהוי אוטומטי בתחילת שורה: `- ` / `* ` + Space → תבליטים, `1. ` / `1) ` + Space → ממוספרת.
+ בטבלה: Tab/Shift+Tab מנווט בין תאים; Tab בתא אחרון יוצר שורה.

---

## 12. undo/redo — כלל חשוב
ב-`exec('undo'|'redo')`:
1. מסירים זמנית את כל `.free-obj` מהעורך.
2. מבצעים את פעולת ה-`execCommand`.
3. מחזירים את האובייקטים שלא חוברו מחדש (`!isConnected`).

**מטרה:** undo לא ימחק תמונות/צורות שלא שייכות לטקסט.

---

## 13. קיצורי מקלדת
| קיצור | פעולה |
|---|---|
| Ctrl+B / Ctrl+I / Ctrl+U | מודגש / נטוי / קו תחתון |
| Ctrl+K | הוסף קישור |
| Ctrl+L / Ctrl+E / Ctrl+R / Ctrl+J | יישור שמאל / מרכוז / יישור ימין / דו-צדדי |
| Ctrl+] / Ctrl+[ | הגדל / הקטן גופן |
| Ctrl+Z | בטל |
| Ctrl+Y / Ctrl+Shift+Z | חזור |
| Ctrl+S | שמור |
| Ctrl+N | מסמך חדש |
| Ctrl+F / Ctrl+H | חיפוש / חיפוש והחלפה |
| Ctrl+P | הדפסה |
| Ctrl+גלגלת | זום |
| Tab / Shift+Tab | הזחה · ניווט בטבלה |
| F9 | תקן שפה — המרת אנגלית↔עברית לפי פריסת מקלדת (בחירה או מילה אחרונה) |
| Escape | סגירת סרגל צף / תפריטים · יציאה ממיקוד/מצגת |

> הערה: קיצורי היישור/גודל-הגופן/קישור פועלים רק כשהפוקוס בעורך (`#editor` או `.tb-content`).

---

## 14. חוקי "Word כבסיס" — עקרונות כלליים
> **Word פתר בעיות אלה מראש.** כשמוסיפים פיצ'ר חדש — לפני כל פתרון אחר, תשאל: "איך Word מתנהג?"

1. **גרירה** — אובייקט נגרר בתוך גבולות הדף בלבד. לא חוצה לדף אחר. תמיד `e.pageX/pageY` (לא client).
2. **שינוי גודל** — מושך handle → אובייקט גדל/קטן, לא עובר דף. Shift בפינה = שמירת יחס.
3. **Snap** — קווי עזר מופיעים בגרירה ליישור (`SNAP_CM = 0.5`); Alt מבטל snap.
4. **מסגרת** — `.page` הוא האזור הנראה. overlays (מספרי עמוד, כותרת) אינם חלק מה-contenteditable.
5. **פונטים** — שינוי גופן/גודל לא "שובר" את מבנה הפסקאות.
6. **הדפסה** — `@media print`: מסתיר סרגלים, overlays עיצוביים, שורת סטטוס, זום, סרגל צף. מדפיס רק את `.page`.

---

## 15. מה אסור לשנות
- **גודל דף:** `21cm × 29.7cm` + שוליים `2.5cm` — **קבוע לעד.**
- **פער בין דפים:** `1.5cm` — **קבוע לעד.**
- **מבנה ה-DOM:** `#editor` הוא ה-contenteditable היחיד. overlays תמיד ב-`#pageStack`, לא בתוך `#editor`.
- **`_clamp()`** — חייב להיקרא לאחר כל גרירה/שינוי גודל/הדבקת אובייקט. **גם בזמן גרירה (mousemove)** כדי למנוע חריגה מהדף.
- **`updatePagination()`** — חייב להיקרא (עם rAF) אחרי כל שינוי תוכן.
- **הוספת קוד:** CSS בסוף ה-`<style>`, JS לפני `buildColorPalette();` — לא להחליף בלוקים גדולים קיימים.

---

## 16. יומן פיצ'רים — שיפורי נוחות בסגנון Word
> נוסף בסשן השיפורים. כל אלה **מומשו ונבדקו בדפדפן חי** (אפס שגיאות קונסול).
> _הרישום המפורט פר-סשן מ-2026-06-22 ואילך מתועד בכספת האובסידיאן `wiki/log.md` (מוח שני); כאן רק הכותרות._

### עיצוב הסרגל הראשי — סבב-תיקונים איטרטיבי 1 (סשן 2026-07-24, סאמר)
> תלונה חוזרת של המשתמש שתיקוני-2026-07-16 (SPEC-DESIGN-RIBBON) לא פתרו בפועל + ממצא-חדש אחד. כל תלונה נבדקה **חי בדפדפן** לפני נגיעה בקוד.
- **בורר-צבעים:** `PRESET_COLOR_NAMES` חדש (20 שמות עבריים, כמו `bdrNames` הקיים) מחובר גם ל-`buildColorPalette` וגם ל-`openColorWheel`; chips 18px→22px + panel 224px→232px; **Live Preview בהצבעה** (`mouseenter`/`mouseleave` על presets/recents מעדכן `.cw-preview`/`.cw-hex` בלי להחיל).
- **טאבים ראשיים:** התיקון השמרני מ-07-16 (`rgba(79,107,237,.06)`) קיים ותקין אך היה בקושי-נראה על זכוכית-הכותרת הבהירה (אומת חי, `getComputedStyle`) — לא כישלון-כיוון. חוזק (**לא שונה הכיוון**, עדיין גוון-אחיד לא-צבעוני): bg `.06→.13`, border `.16→.34`, נוסף `border-bottom` 2px כעוגן שלישי.
- **אי-אחידות בלשונית "בית" (ממצא חדש):** רק קבוצת "גופן" הייתה `.group-titled` (label+box, כמו כל שאר הסרגלים); "סגנון"/"פסקה"/"רשימות"/"בטל-בצע" היו `.group` שטוח בלי label. הומרו כל 4 ל-`.group-titled` (מבנה בלבד, IDs/פונקציונליות ללא שינוי).
- אומת חי (screenshots+computed-style) לפני/אחרי; מרקוס `home-ribbon` 46/46 + `insert` 75/75 (×3 ריצות), 0 שגיאות console.
- **פתוח לאיטרציה הבאה:** גלריית "Theme Colors" בסגנון-Word-מלא (עמודות=בסיס-צבע, שורות=tint/shade) — שינוי-מבנה-נתונים גדול יותר, לא בוצע; אם התלונה על הצבע נמשכת, לשאול את המשתמש מה עוד חסר באופן ספציפי.

### תמונה — חיתוך לגבולות האובייקט אחרי הסרת-רקע (סשן 2026-07-20)
> בקשת-משתמש: "לתפוס את האובייקט ללא רקע ולהקטין או להגדיל". add-only.
- **השורש:** `.free-img-wrap img { object-fit: fill }` (~664) — התמונה **תמיד** ממלאת את ה-box של ה-`.free-obj`. לכן אחרי הסרת-רקע המסגרת והידיות חובקות את **מלבן-המקור כולו** כולל השוליים השקופים; המשתמש גורר ומשנה-גודל של אוויר.
- **המימוש:** `_imgOpaqueBounds(ctx,w,h)` (גבולות הפיקסלים עם `alpha>8`, `null` = הכל שקוף) · `_imgCropToBounds(obj,cv,b)` · `_imgRestorePreCrop(obj)` — כולם ליד `_imgEnsureOrig`. תיבת-סימון "✂️ חתוך לגבולות האובייקט" ב-`openBgRemovePanel`, **דלוקה כברירת-מחדל**, פועלת ב"החל".
- **⚠️ חיתוך-פיקסלים לבדו שובר את התצוגה:** חייבים לכווץ `width/height` של ה-`.free-obj` **באותו יחס** ולהזיז `left/top` ב-`W*x0/nw` — אחרת התמונה נמתחת. לאובייקט **מסובב** מרכז-החיתוך חייב לעבור את אותה רוטציה סביב המרכז הישן, אחרת הוא קופץ בחיתוך א-סימטרי.
- **⚠️ "שחזר תמונה מקורית" חייב להחזיר גם גיאומטריה** — יש **3** מסלולי-שחזור (פאנל-מסננים · פאנל-הסרת-רקע · ריבון `imgformat`), כולם קוראים ל-`_imgRestorePreCrop`. הגיאומטריה ב-`dataset.preCropBox`, מסוריאליזת כ-**`imgPreCrop`** — בלעדיה, אחרי רענון השחזור מותח את התמונה המלאה לתוך ה-box המכווץ.
- אומת חי: תמונה 100×100 עם ריבוע ב-(30,20,40,25) → פיקסלים 40×25, box 40×25, `left/top` 68/58 — בדיוק הצפוי; round-trip סריאליזציה; שחזור מחזיר 100×100. מרקוס `insert` 75/75 · `free-objects` 19/19 · `page-rules` 18/18.
- ⚠️ **בבדיקות:** `insertFreeImage()` **לא מקבל src** (פותח בורר-קבצים) — להשתמש ב-`_buildImageObjFromSrc(src)`.

### תפריט קליק-ימני (`#rcMenu`) — 5 תיקונים + suite `rcmenu` (סשן 2026-07-20)
> IIFE `initRightClick`. עד לסשן הזה: **אפס כיסוי-בדיקות** ל-138 שורות. פירוט: wiki/log.md.
- **★★ P1 — `preventDefault` הגורף הרג את בודק-האיות.** אין ב-MyWord מנוע-איות משלו: `spellcheck` הוא נייטיב של הדפדפן, ותפריט-הדפדפן הוא ה**מסלול היחיד** להצעות-תיקון/"הוסף למילון". התוצאה: הקו-האדום הופיע ואי-אפשר היה לתקן דרכו. **תוקן:** סמן-מכווץ בטקסט רגיל → משאירים את תפריט-הדפדפן (וכבונוס מקבלים הדבקה מלאת-עיצוב, שההדבקה שלנו `insertText` לא נותנת); **בחירת-טקסט / תא-טבלה / קישור → התפריט שלנו**, שיש בו ערך מוסף. כיבוי `#tlSpell` מחזיר את התפריט שלנו גם על סמן-מכווץ.
- **★ P2 — `.tb-content` נבלע ב-guard.** `if (e.target.closest('.free-obj')) return;` נועד לתמונה/צורה (שיש להן סרגל-צף) אך תפס גם עריכת-טקסט אמיתית בתיבת-טקסט/פתק — בלי גזור/העתק/עיצוב. נוסף חריג. **תמונה/צורה נשארו בכוונה ללא תפריט** (סרגל-צף).
- **P4 — נגישות (W3C APG):** `role=menu` / `menuitem` / `separator`, ניווט חצים+Home/End, Enter מפעיל. **⚠️ ההדגשה היא `class` + `aria-activedescendant` ולא `focus()`** — הזזת הפוקוס אל הכפתור מוציאה אותו מה-contenteditable ושוברת את `execCommand` של הפריטים (בדיוק הסיבה שכל כפתור עושה `preventDefault` ב-`mousedown`).
- **P5 — הקשר-קישור:** קליק-ימני על `<a>` הציג "הוסף קישור". הוחלף בפתח / העתק-כתובת / ערוך / הסר (הסרה = `selectNodeContents`+`unlink`, **לא** `remove()` — אחרת נמחק גם טקסט-המשתמש, אותו לקח כמו מחיקת-סימנייה 07-18). `ערוך` חוסם `javascript:`.
- **P6 — מיקום:** היה LTR (גדל ימינה מהסמן). עכשיו RTL כמו Word עברי — הקצה הימני בסמן, גדל שמאלה, מתהפך כשאין מקום. מקש-התפריט של Windows/Shift+F10 שולח `clientX/Y=0` והתפריט קפץ לפינה — נופל עכשיו למלבן-הבחירה.
- **suite `rcmenu` — 7 תרחישים התנהגותיים** (לא "האלמנט קיים"). **הוכחת-אדום:** מול הגיבוי שלפני-התיקון — **2/7 עם 15 ממצאים**; אחרי — 7/7.
- **מלכודת-בידוד:** `#rcMenu` הוא singleton קבוע ב-`<body>` ואינו `.mw-panel`, ולכן `__reset()` לא ניקה אותו — תפריט פתוח דלף לתרחיש הבא ושם הקליק-הראשון רק *סגר* אותו. אותה מלכודת כמו `#bookmarkNavPanel` (07-18). נוסף ניקוי ל-`__reset`.

### טבלה — AutoFit · גלריית-סגנונות · מיון · כותרת-חוזרת (סשנים 2026-07-18/19)
> 4 פערי-Word שאומתו ב-qa-sweep. add-only: נשענים על `colgroup`+`table-layout` הקיימים, לא נגעו באף נתיב-גרירה/עימוד. פירוט: wiki/log.md "2026-07-18".
- **AutoFit — 3 מצבים** (`_setTableAutofit` + `openTableAutofitPanel` + `TABLE_AUTOFIT_MODES`, פאנל `.wrap-panel` כמו בורר-הגלישה): `contents` (`table-layout:auto`+ניקוי רוחבים) · `window` (100%, עמודות שוות) · `fixed` (הקפאת הרוחבים המרונדרים ל-px). מצב `fixed` מיישם **read-all-then-write-all** — הלקח מ-`_applyTableSize` (2026-07-12).
- **גלריית-סגנונות** (`_applyTableStyle` + `openTableStylePanel` + `TABLE_STYLE_PRESETS`): 7 ערכות (plain/grid/indigo/blue/green/warm/lines) לטבלה קיימת.
- **מיון** (`_sortTable` + `openTableSortPanel`): לפי עמודה, עולה/יורד, מספרי או טקסטואלי, עם דגל "יש שורת-כותרת". **נעול על תאים-מאוחדים** (`_tableHasMergedCells` מחזירה false ולא נוגעת בשורות).
- **כותרת חוזרת בכל עמוד** (`data-repeatHeader`): ב-`_paginateTables`, אחרי הכנסת `_pgspacer`, שורת-הכותרת משוכפלת לראש העמוד החדש. **★ עיצוב-הליבה שהפך את זה לבטוח:** השכפול נושא **גם** את `_pgspacer` (`class="_pgspacer _pghdr"`) → כל 9 אתרי-הניקוי הקיימים (`querySelectorAll('tr._pgspacer')` בשמירה/ייצוא/הדפסה/`_stripSpacers`) מסירים אותו **בלי שנגעו בהם**, והוא לעולם לא נצרב לתוכן. עיצוב inline עם `!important` מ-snapshot יחיד פר-טבלה (לא בנתיב-החם).
- **★★ באג-אמת שנתפס:** `_sortTable` הריץ את בדיקת התאים-המאוחדים **לפני** הסרת ה-spacers — ושורת-ריווח מכילה `<td colSpan=N>`, כלומר נראית כתא-מאוחד → **המיון נחסם בכל טבלה שחוצה עמוד** (המקרה השימושי ביותר). תוקן בסדר-הפעולות + סינון `_pgspacer` בתוך `_tableHasMergedCells`.
- **מגבלה מוצהרת:** הדפסה/ייצוא **לא** מקבלים את הכותרת החוזרת (השכפולים מוסרים בניקוי, `<thead>` נייטיב לא הוטמע). המשך מומלץ: המרה ל-`<thead>` בשכפול-הייצוא בלבד.

### תמונה — גלריית סגנונות מוכנים + דחיסה (סשנים 2026-07-15 / 2026-07-18)
- **סגנונות מוכנים** (`IMG_STYLE_PRESETS` + `openImageStylesPanel`, כפתור 🎴 ב-ctxBar-"עוד" ובריבון `imgformat`): מחיל מסגרת+פינות+צל+סיבוב-קל במקש אחד, כמו Picture Styles בוורד.
- **דחיסת תמונה** (`openImageCompressPanel`, כפתור 🗜️ בשני המקומות): מזהה שקיפות (`_imgHasTransparency`), מציע רזולוציית-יעד פי-2 מגודל-התצוגה בפועל (בלי הגדלה, רצפת 64px), ומחוון איכות-JPEG כשאין ערוץ-אלפא.
- **תיקון 2026-07-15:** `openImageStylesPanel` ו-`openImageBorderPanel` מעולם לא קראו ל-`_imgEnsureOrig` — ולכן "שחזר תמונה מקורית" (guard על `origSrc`) היה **מושבת לחלוטין** לתמונה שרק קיבלה מסגרת/סגנון ולא נגעה במסננים. נוספה הקריאה בשניהם.

### הרחבת תיבת-טקסט חופשית (סשן 2026-07-16ב — סאמר, 8/10 פערים מול Word/Google Docs/Canva)
> add-only, כל החדש בעמוד-2 של "עוד" ב-ctxBar (חוק 2-השורות §8). פירוט מלא + טבלת-מיפוי: `docs/SPEC-INSERT-TEXTBOX.md` §"הרחבה 2026-07-16"; wiki/log.md "2026-07-16ב".
- **מסגרת מתקדמת** (`openTextboxBorderPanel` — עובי/סגנון/צבע, כותב ל-`obj.style.border` הקיים, אין הגירת-סכמה) · **מרווח פנימי** (`openTextboxPaddingPanel`, שדה `tbPadding`) · **יישור-טקסט אנכי** (`window._setTbValign`, flex על `.tb-content`, שדה `tbValign`).
- **Autofit — 3 מצבים כמו Word** (`window._setTbAutofit('auto'|'fixed'|'shrink')` — `fixed`/`shrink` הופכים לתיבה ל-flex עם `.tb-content{flex:1;overflow:hidden}`; `shrink` מוסיף כיווץ font-size הדרגתי עד 8px, שדה `tbAutofit`).
- **כיוון-טקסט אנכי** (`window._toggleTbVertical`, `writing-mode:vertical-rl`, שדה `tbVertical`) · **מיקום/גודל/זווית מדויקים** — שימוש-חוזר מלא ב-`openShapeTransformPanel` הגנרי הקיים (אפס קוד חדש).
- **צל** (`openTextboxShadowPanel`+`window._applyTbShadow`, `box-shadow` על ה-obj עצמו, שדה `tbShadow`) · **Format Painter לתיבה** (`window._captureTbStyle`/`_applyTbStyle` — רקע/מסגרת/צל/שקיפות/padding) · **צורת-מיכל ויזואלית** (`openTextboxClipPicker`+`window._applyTbClip`, `clip-path` בלבד — bounding-box/text-flow נשארים מלבניים, לא `shape-outside`, שדה `tbClip`).
- ~~**דולג במכוון (out-of-scope מדווח):** קישור בין תיבות/זרימת-טקסט אוטומטית~~ — **בוטל: מומש ב-2026-07-18** (ר' הסעיף הבא). ההחלטה המקורית נבעה מהנחה שהפיצ'ר דורש שינוי בליבת ה-pagination/save; בפועל `.tb-content` הוא contenteditable מחוץ-לזרם ולכן המימוש לא נגע ב-`paginateBlocks`/`_clamp`/`updatePagination` כלל.
- אומת חי (Playwright, לא syntax-בלבד): כל 8 הפאנלים + round-trip serialize→deserialize מלא. מרקוס `insert` 75/75 + `free-objects` 14/14 + `page-rules` 18/18 — כולם זהים לבייסליין, 0 שגיאות console.

### קישור בין תיבות-טקסט — זרימת-טקסט אוטומטית (סשן 2026-07-18; תיקון 2026-07-19)
> **הופך את ההחלטה המתועדת מ-2026-07-16ב** ("§9 נדחה במכוון"). המימוש add-only ואינו נוגע בליבת-העימוד. פירוט: wiki/log.md.
- **מנגנון:** `dataset.linkedNextId`/`linkedPrevId` (id דרך `_connEnsureId`, אותו מנגנון כמו מחברים). `_tbStartLinkPick(obj)` נכנס למצב-בחירת-יעד → הקליק הבא על תיבת-טקסט (עטיפה **רביעית** על `_selectObj`) קורא ל-`_tbFinishLinkPick`; Escape מבטל. `_tbWouldCreateCycle` חוסם שרשרת-מעגלית (guard<200). UI: 🔗/✂️ בעמוד-2 של "עוד" ב-`buildTextbox`.
- **זרימה:** `_tbFlowOverflow(src)` מעבירה (`insertBefore(lastBlock, firstChild)` — העברה אמיתית, לא שכפול, וסדר-הבלוקים נשמר) את הבלוק האחרון מ-`.tb-content` של המקור לראש היעד, בלולאה עד שהמקור נכנס (guard<40, כמו `_tbShrinkToFit`). מופעלת ממאזין-delegation יחיד על `input` ב-`#editor` (לא מחליף את מאזיני-ה-`scheduleSave` הקיימים). שרשרת ממשיכה רקורסיבית.
- **⚠️ תלות קריטית ב-Autofit (תוקן 2026-07-19):** התנאי לזרימה הוא `scrollHeight > clientHeight`. בברירת-המחדל (`tbAutofit='auto'`) ה-`.tb-content` הוא `overflow:visible` והתיבה גדלה עם הטקסט → **התנאי לעולם לא מתקיים והקישור נשאר חסר-השפעה בשקט**. לכן `_tbFinishLinkPick` מעביר תיבת-מקור שאינה `fixed`/`shrink` ל-`fixed` אוטומטית (דרך `_tbApplyAutofit`, בלי `_historyRecord` כפול) ומודיע על כך ב-`showHint`. כמו ב-Word, תיבה מקושרת היא בגודל קבוע.
- **שדות סריאליזציה:** `linkedNextId`/`linkedPrevId` ב-`_serializeObj` + שחזור ב-`_deserializeFreeObjs`. Undo עובד "בחינם" (snapshot-based).
- אומת חי: קישור · חסימת-מעגל · Escape · זרימה של 11 בלוקים בסדר נכון ממצב-ברירת-מחדל · שימור `shrink` · round-trip serialize→deserialize · ביטול-קישור. מרקוס `insert` 75/75 + `free-objects` 19/19, 0 שגיאות console.

### qa-sweep — 7 פריטים מהסקירה (סשן 2026-06-26 — ponytail-everything + qa-flow)
> כולם **אומתו חי בדפדפן** (preview 3001, אפס שגיאות קונסול). עריכות בוצעו בסקריפט-Node אטומי כי OneDrive/preview שכתב את הקובץ בין כל Read ל-Edit (ראה זיכרון [[onedrive-file-swapping]]).
- **#1 צורות — `unite` + `exclude` (XOR) ב-Paper.js אמיתי:** `paperBooleanOp` הורחב ל-4 פעולות; `combineSelectedShapes` ממפה `'union'→'unite'` ומנתב את כל הארבעה דרך Paper.js (לא עוד overlay-SVG ל-union). כפתור ⊕ "חפיפה הפוכה" נוסף לבחירה-מרובה. fallback ל-mergeSelectedShapes רק כש-Paper לא טעון.
- **#3 טבלה — `#editor table { max-width: 16cm }`:** חוסם טבלאות מודבקות/רחבות מלחרוג מאזור-התוכן (משלים את clamp ה-`_colDrag` שעבד רק בזמן resize).
- **#4 התנגשות בחירת עמודה/שורה — אין באג:** הארכיטקטורה הנוכחית (`#tblSelBar` עם צ'יפים ממוספרים, כל אחד עם `stopPropagation`) חסרה את "חץ-התפריט" שהבאג תיאר. לא נדרש שינוי.
- **#5 מסגרת תאים — `openCellBorderPanel`:** כפתור `SIC.frame` ב-`buildTableMulti` פותח `.mw-panel` עם סגנון(solid/dashed/dotted/double/none)·עובי(1-5px)·צבע. מחיל border-style/width/color אינדיבידואלי על התאים הנבחרים. (הערה: border-collapse → על קצה-חיצוני מול תא לא-נבחר חלה הכרעת-CSS.)
- **#6 מחיקת סימנייה:** `initBookmarkNav` שוכתב ל-`paint()` שמרענן מה-DOM; כל שורה קיבלה כפתור 🗑 (`.bmnav-del`) שמסיר את `.bookmark` + `_historyRecord`/`scheduleSave`/`updatePagination`.
- **#7 דף כהה — opt-in (לא הופך החלטה מתועדת):** מתג `#vwPageDark` ב"תצוגה"; `body[data-theme="dark"].page-dark` עוקף `--page-bg/--page-text`. ברירת מחדל **כבוי** → §view "הדף נשאר לבן" נשמר. בלי filter:invert → תמונות/צורות לא נפגעות. נשמר ב-`myword2_pagedark`.
- **#2 העברת טבלה — DOM reorder (בטוח ל-v2, לא free-drag):** `initTableMove` מציג ידית ⠿ (`.tbl-move-handle`, fixed, בשול הימני מחוץ לטבלה) ב-hover; גרירה מעבירה את הטבלה בין בלוקי-האחים עם סמן-הזחה. נשארת בזרם → שמירה (innerHTML) ועימוד תקינים; `stopPropagation` שומר על `_colDrag`/בחירת-קצה. **פתוח:** גרירה-חופשית (absolute + סריאליזציה + עימוד) לא מומשה — דורשת סשן ייעודי.
- **ידית-גרירה ⠿ לכל אובייקט חופשי (אופציה-2 של המשתמש):** `_addHandles` מוסיף `.free-move-handle` (⠿) לתמונה/תיבה/צורה/ציור — נקודת-אחיזה ברורה בשול החיצוני (RTL: ימין), מופיעה ב-hover/בחירה, נעלמת ב-locked/הדפסה/ייצוא. גרירה דרכה = אותו `_makeDraggable(obj, mvh)` (כולל גלילה-אוטומטית); `stopPropagation` על ה-mousedown מונע כפל-טריגר מהמאזין של האובייקט. לא מסוריאליזת (כמו שאר הידיות — `_serializeObj` בונה מ-props). אומת חי: מופיעה/נגררת (Δ60/80), צורה+תמונה, אפס שגיאות.
- **גלילה-אוטומטית בגרירה (כל האובייקטים + ידית-טבלה):** עזר משותף `_edgeAutoScroll*` — כשהסמן ≤70px מקצה `#editorArea` (מיכל-גלילה פנימי overflow:auto) גוללים אותו (rAF, מהירות מדורגת עד 24px/frame) כך שהאובייקט הנגרר תמיד נראה. אובייקט-חופשי: `_freeDragScrollComp` מפצה `_dragSt.startY -= applied` ומריץ `_onGlobalMove` מחדש (pageY לא משתנה בגלילה פנימית) → האובייקט נשאר תחת הסמן (מצבי wrap/inline מדולגים, נעולים לעמוד). טבלה: reapply מחשב מחדש את סמן-ההזחה. אומת חי: down +69/up −46, אמצע=0, עצירה נקייה, אפס שגיאות. (free-obj follow אומת לוגית — `_dragSt` הוא closure-let, לא ניתן לסימולציית-מאוס מ-eval; הנתיב זהה ל-mousemove רגיל.)

### סרגל צף — סידור דו-שורתי אחיד + תיקון חיתוך (סשן 2026-06-24 — browser-debug + qa-flow + ponytail-everything)
> אומת חי בכל ארבעת ההקשרים (טקסט/תיבה/טבלה/תמונה) + קרוסלה + מעבר-לשונית. מרקוס 773/773 ✅, אפס שגיאות console.
- **כפתור "עוד" העגול עבר לשורה-2 ממורכז בין שתי חצאי-קבוצות** (`_layout` ב-`initCtxBar`): `cmds.has-rows` = עמודה של `ctx-row1` (כלים ראשיים) + `ctx-row2` שמכיל `ctx-half-a` / הכפתור העגול / `ctx-half-b` — פורט מ-`docs/floatbar-v04-decoded.html` ומהסרגל הנייד `#mobBar`. קרוי דרך `moreBtn`→`_layout` (כל 4 הבונים) ומשוחזר ב-`togglePage`. עד 6 כלים בשורה-2; השאר בשורה-1.
- **תיבת-טקסט: הוסרה שורת-הגלילה האופקית** (`ctx-scroll-row/inner/slide-btn` — JS+CSS) שגרמה למראה שונה משאר הסרגלים. כעת אותו סידור דו-שורתי. (מבטל את הבולט "תיקון גלילה אופקית" מ-2026-06-23.)
- **תוקן חיתוך-הסרגל מחוץ למסך:** `positionRect` קבע `left` פיקסלי אך לא איפס את `transform:translateX(-50%)` הבסיסי → כל סרגל רחב (600px) נדחף 300px שמאלה ונחתך ברוחב-דסקטופ צר. תוקן: `bar.style.transform='none'` ב-`positionRect`; שחזור ב-`buildInsertPrompt` (מצב-ברירת-מחדל ממורכז).
- **מעבר-לשונית בלחיצה יציב:** `switchTab` משחרר אובייקט נבחר במעבר ל"כתיבה" + `_tabLock` (400ms) מונע מ-`refreshCtx` להחזיר מיד את ההקשר הקודם.
- **סידור סמנטי + עד 2 שורות (לפי צילום-עיצוב של המשתמש):** הוסף `rowBreak()` — סמן `<i.ctx-rowbreak>` (מוסתר, שורד מעבר-עמוד) שהבנאי מציב בין שורה-1 לשורה-2; `_layout` מפצל לפיו (לפניו→row1, אחריו→row2) ובהיעדרו נופל לחלוקה-מאוזנת. `buildText` סודר מחדש: **שורה-1 = כלי תו/גופן** (סגנון·גופן·גודל·B·I·U·S·מרקר·צבע), **שורה-2 = כלי פסקה** (4 יישורים | כפתור-עגול | תבליטים·ממוספרת·כניסה±). עמוד-2 (תחת הכפתור) = גופן±·קישור·עילי/תחתי·ניקוי. מחוון-שקיפות יורד לפס דק נפרד (`.has-rows > .ctx-opacity`). רוחב הסרגל הורחב ל-`#ctxBar 716px` / `.tb-body min(680px)` כדי שהתיבה הצפופה תיכנס ל-2 שורות. `_gatherFlat` גורף **כל** פריט (כולל סליידר-שקיפות, לא רק `<button>`).
- **שרת-תצוגה (`.claude/static-server.mjs`):** נוספו כותרות `Cache-Control:no-store` — מנע מצב ש-localhost:8099 מציג גרסה ישנה מ-cache הדפדפן אחרי עריכת קוד.

### סרגל צף + תמונה (סשן 2026-06-23 — browser-debug + qa-flow)
> אומת חי, אפס שגיאות. **שים לב:** בקוד יש **שתי מערכות סרגל-צף** — `#ctxBar` הישן + פורט `floatbar-v04` (סביב שורה 14279, עיצוב glassmorphism מ-`docs/floatbar-v04-decoded.html`). זה מקור "כפילות הכפתורים".
- **גלגלת מעל הסרגל הצף מחליפה לשונית** — כתיבה→תיבת טקסט→טבלה→תמונה (מחזורי, throttle 320ms). `Ctrl+גלגלת` נשאר לזום (לא מחליף).
- **מעבר-סטֵג לא מוסיף אובייקט אוטומטית** — גלילה ללשונית טבלה/תמונה/תיבה מציגה כפתור "הוסף"; המשתמש מחליט מתי להכניס.
- **כפתור ▾ (split) לטבלה בסרגל הרגיל** — שיקוף פעולות הסרגל הצף לסרגל הראשי דרך החץ.
- **כותרות → כפתור split יחיד** (במקום H1/H2 נפרדים) + הסרת שמות-אייקונים (סימונים בלבד), לפי המקור.
- **תיקון גלילה אופקית בסרגל הצף:** `overflow-x:scroll + direction:ltr` על `.ctx-scroll-inner` וכו'. **⚠️ הוסר ב-2026-06-24** — שורת-הגלילה הוחלפה בסידור הדו-שורתי האחיד; אל תשחזר.
- **תמונה float אופקי תוקן:** `marginRight` ננעל בטעות ב-2.5cm במקום 1.0cm → התמונה לא הגיעה לגבול המוגדר. תוקן ואומת (מדידה אמפירית).
- **פתוח (לא הושלם):** התאמת glassmorphism מלאה 1:1 בכל מצבי הסרגל · float **אנכי** (בין שורות) לא שוחזר · עטיפת-טקסט (float) סביב **טבלה** — פיצ'ר חסר · **קריינים (TTS):** בחירת קול לא משנה את הקול בפועל; החלטת API (Google/Azure מומלץ / ElevenLabs / Piper) ממתינה למשתמש.

### שדרוג — שלב 2 (סשן 2026-06-12c — פיצ'רים מקצועיים)
> נבדק חי + מרקוס מלא ירוק. אפס תלויות. JS לפני buildColorPalette, CSS בסוף style.
- **כיוון חכם — תו-חזק-ראשון:** `_firstStrongDir(text)` מחזיר rtl/ltr/null לפי התו הכיווני-החזק הראשון (כמו Word/UAX#9). מחליף את ספירת-הרוב (`detectScript`) ב-`autoDetectDirAll` + מאזין ה-input. פסקה עברית עם ציטוט אנגלי ארוך **לא מתהפכת**. `detectScript` נשאר ל-F9 בלבד.
- **בורר צבע מקצועי `openColorWheel(opts)`:** גלגל גוון/רוויה (canvas, hue=זווית, sat=רדיוס) + מחוון בהירות (value, שכבת-עמעום שחורה) + שדה HEX + צבעים אחרונים (`myword2_recent_colors`). עוזרים: `_hsvToRgb`/`_rgbToHsv`/`_hexToRgb`/`_rgbToHex`. מחובר ל-3 מקומות: צבע-טקסט + הדגשה (החץ ▾ של `#colorBtn`/`#bgColorBtn`, capture listener שמדכא את פלטת-הבועות הישנה) + רקע-דף מותאם (`#loBgCustom`, תצוגה חיה). `opts`: `{anchor,color,onPick,onLive,showNone,title}`. נסגר ב-`_mwPanelDismiss`.
- **הקראה — קריינים:** `initSpeak` עם `prefs={voiceURI,rate,pitch}` ב-`myword2_tts`. כפתור חץ `#tlVoiceOpts` פותח פנל: בחירת קול (`speechSynthesis.getVoices()`, מקובץ he/en, `onvoiceschanged`), מחווני קצב (0.5–2) וגובה (0–2), כפתור "נסה". `voiceFor(lang)` בוחר קול תואם-שפה לכל משפט.

### שדרוג — שלב 1 (סשן 2026-06-12b — ארגון מחדש, בהירות, מיקרופון)
> בעקבות סקירת משתמש של הלשוניות החדשות. מרקוס מלא ירוק. החלטות מאושרות בתוכנית.
- **מצגת הוסרה לגמרי** — IIFE `initPresentation` + כפתור `#vwPresent` + CSS `#presoOverlay` + `window.exitPreso`. ה-`__reset` ב-qa כבר לא מנקה מצגת (תיקון `_mwPanelDismiss` self-removal מ-§8 נשאר — עצמאי).
- **רקע דף עבר מ"פריסה" ל"תצוגה"** — רק מיקום ה-HTML (קבוצת `loBg*`); ה-JS (`applyPageBg` + עטיפות saveDoc/loadDoc, שדה `pageBg`) ללא שינוי. פריסה נשארה: כיוון טקסט, רווח פסקה, אוטו-עיצוב.
- **ייצוא עבר מ"כלים" ל"קובץ"** — קבוצת Word/PDF/HTML מאוחדת בלשונית קובץ (onclick → `exportDocWord`/`exportAndPrint`/`exportHTML`; `window.exportDocWord` נחשף). מזהי `tlExport*` + המאזינים שלהם הוסרו.
- **הכתבה קולית — מיקרופון:** בלחיצה: אם `!window.isSecureContext` → הנחיה ברורה (file:// חוסם מיקרופון); אחרת `navigator.mediaDevices.getUserMedia({audio:true})` מפורש להצפת ה-prompt לפני `rec.start()`. `onerror` מטפל גם ב-`network`/`audio-capture`. ⚠️ פתיחה כקובץ מקומי (file://) חוסמת מיקרופון מסיבות אבטחה — דרוש https/localhost.
- **בהירות:** tooltips עשירים + hints מנחים עם תנאי-קדם (קטעים: "סמן טקסט לפני פתיחת הפנל"; תוכן עניינים: "החל סגנון כותרת ואז לחץ שוב"; אוטו-עיצוב: דוגמאות קונקרטיות).

### באג שתוקן (סשן 2026-06-12 — דליפת מאזין-Escape של פנלים)
> נמצא בלולאת QA של הלשוניות החדשות (מרקוס 710/710 → ירוק אחרי התיקון).
- **דליפת capture-Escape מ-`_mwPanelDismiss`** — `openStatsDialog`/`openSnippetsPanel`/`openAutoFormatPanel` מסירים פנל קודם ב-`.remove()` בלי `close()`. המאזין שנרשם ב-capture (אחרי 50ms) עם `stopPropagation` על Escape **נשאר מחובר** אחרי הסרת אלמנט הפנל → בולע Escape גלובלי, וכל פתיחה חוזרת מוסיפה עוד מאזין דולף. התסמין: אחרי פתיחת פנל, Escape הפסיק לצאת ממצב מיקוד/מצגת. **תוקן** ב-`_mwPanelDismiss`: המאזין מסיר את עצמו כש-`!panel.isConnected` ולא בולע, וה-`setTimeout` מדלג אם הפנל כבר נסגר. ראה §8.
- **בידוד תרחישי מרקוס** — `window.exitPreso` נחשף (כמו `startPresentation`/`toggleFocusMode`); `__reset` ב-`qa/lib/scenarios.mjs` מנקה כעת `#presoOverlay`+`.mw-panel`+`focus-mode`/`hide-status` בין תרחישים. תרחישי `setup` חדשים — **סינכרוניים בלבד** (אסור `setTimeout`+`throw`, זורק pageerror אסינכרוני).

### פיצ'רים חדשים (סשן 2026-06-11 — 3 לשוניות + תקן-שפה)
> נבדק חי + מרקוס 216/216. כל ה-JS לפני buildColorPalette, CSS בסוף style, אפס תלויות.
- **לשונית פריסה** — כיוון טקסט rtl/ltr + "כיוון חכם" (זיהוי שפה פר-פסקה, גם בהקלדה); רקע דף (presets+מותאם, נשמר בשדה `pageBg` דרך עטיפה שלישית של window.saveDoc/loadDoc, לבן בהדפסה); רווח פסקה לפני/אחרי 0-24pt (inline style — גובר על margin:0, נשמר עם content); פנל אוטו-עיצוב (גרשיים חכמים, --→–, URL→קישור, ---→hr, ‏**md**, נשמר ב-`myword2_autofmt`).
- **לשונית כלים** — toggle איות; סטטיסטיקה+יעד מילים (מד התקדמות `#goalProgress` בסטטוס, `myword2_goals`); קטעים שמורים (`myword2_snippets`); תוכן עניינים (`div[data-mwtoc]`, קישורים עם scrollIntoView); ייצוא Word ‏(.doc=HTML ב-Blob, בלי ספרייה) / PDF (print) / HTML; הכתבה קולית (webkitSpeechRecognition he-IL, restart אחרי שתיקה, מושבת אם אין API); הקראה (speechSynthesis, שפה פר-משפט).
- **לשונית תצוגה** — ערכת נושא כהה (`body[data-theme=dark]`, UI בלבד — הדף נשאר לבן, `myword2_theme`); מצב מיקוד (מסתיר סרגלים, Esc); מצב מצגת (שקופית לכל H1/H2 או כל 3 פסקאות, fullscreen, z-index 10060, fullscreenchange ליציאה); זום (מפנה לפקדי הסטטוס); הסתרת שורת סטטוס.
- **תקן שפה (F9)** — המרת תווים אנגלית↔עברית לפי פריסת מקלדת ישראלית (`KBD_EN2HE`/`KBD_HE2EN`). חכם: בחירה אם יש, אחרת המילה האחרונה לפני הסמן; זיהוי כיוון אוטומטי; המרה ב-TreeWalker על text nodes (עיצוב נשמר); בחירה-מחדש בסוף → F9 שוב ממיר חזרה; snapshot ל-undo דרך `window._historyRecord` (נחשף מ-initUndoHistory).
- **עזרים חדשים:** `detectScript(text)` (he/en/none), `_mwPanelDismiss(panel)` (קליק-חוץ+Esc), `.mw-panel` (פנל גנרי z-index 9990).

### פיצ'רים קודמים
- **קיצורי מקלדת מלאים** — `Ctrl+K` קישור, `Ctrl+L/E/R/J` יישור, `Ctrl+]`/`[` גודל גופן, `Ctrl+N` חדש, `Ctrl+Y`/`Ctrl+Shift+Z` חזור. פועלים רק כשהפוקוס בעורך (`#editor`/`.tb-content`).
- **מברשת עיצוב** (`#btnFormatPainter`) — לוכד עיצוב מטקסט ומחיל על הבחירה הבאה (חד-פעמי).
- **רשימות אוטומטיות** — `1. `/`1) ` → ממוספרת, `- `/`* `/`• ` → תבליטים (אחרי Space בתחילת שורה).
- **ניווט טבלה ב-Tab** — Tab/Shift+Tab בין תאים; Tab בתא אחרון יוצר שורה.
- **זום תצוגה** — מחוון בשורת הסטטוס (50%–200%) + Ctrl+גלגלת. מתמטיקת גרירה/שינוי-גודל מחלקת ב-`window._docZoom`.
- **תפריט קליק-ימני** (`#rcMenu`) — גזור/העתק/הדבק, עיצוב, קישור, ופעולות טבלה.
- **מחוון שקיפות אמיתי** (`.ctx-opacity`) 0%–100% לתמונה/תיבת טקסט + כפתור מהיר 🔽 "מאחורי הטקסט".
- **מחטא הדבקה** (`initPasteSanitizer`) — מנקה HTML מ-Word/אינטרנט, מסיר `style/class/script/href="javascript:"`, מדביק תמונות מהלוח כאובייקט חופשי.

### באגים שתוקנו (סשן 2026-06-10 — עימוד)
> נבדק חי על מסמך המשתמש האמיתי + **מרקוס 216/216 (כל החבילות)** + 6 בדיקות ממוקדות. אפס שגיאות.
- **רשימה ילד-ישיר חצתה עמודים (רגרסיה מ-splitContainer)** — דחיפת LI במרגינים הסתירה את החצייה מ-`_paginateLists` (המפצל הפיזי). תוקן: רשימות ילד-ישיר נשארות ל-`_paginateLists`; splitContainer מטפל רק ב-DIV/רשימות מקוננות. ראה §4 "חלוקת-עבודה".
- **17/6 עמודים לתוכן של ~4 עמודים** — שני שורשים:
  1. **נוסחת `naturalPages` חילקה ב-`pageH` במקום `cycle`** — `scrollHeight` כבר כולל את דחיפות ה-gap מ-`paginateBlocks`, אז חלוקה ב-`pageH` ניפחה. תוקן ל-`ceil((scrollHeight+gap)/cycle)` ב-`updatePagination`, `_pageCounts`, ובמטפל ה-delete.
  2. **מיכל DIV מקונן (מהדבקה) נדחף שלם** — DIV בגובה ~1055px (כמעט עמוד) שחוצה גבול נדחף כיחידה אחת והשאיר חצי עמוד ריק → עוד עמודים. תוקן ע"י `splitContainer()` רקורסיבי שמפצל מיכלים ברמת הילד (ראה §4).
- **`manualPages` תקוע גבוה (עמודים ריקים שנשמרו)** — `loadDoc` מקלמפ ל-`_pageCounts().natural` בעת טעינה; `updatePagination` מאפס אם `> natural*2+3`.
- **`data-pagebreak` cascade** — נדחף 12 פעמים בלולאה. תוקן ב-`!data-pushed`. פסקת שובר ריקה (גובה 0) שוב מעובדת (לא נדלגת ב-`!h`).
- **Enter מוריד הרבה טקסט** — `#editor p, #editor div { margin: 0; }` בסוף ה-`<style>`. מונע gap ברירת-מחדל של הדפדפן (16px) בכל לחיצת Enter.
- **גרירה חורגת מגבולות הדף** — `_clamp()` נקרא כעת גם ב-`mousemove` (לא רק ב-`mouseup`). מונע "החלקה" מחוץ לדף.
- **FREE_MARGIN_CM: 0.2 → 0.5 → 1.0** — כל אובייקט חופשי (צורות/תיבות) נשאר 1.0 ס"מ (≈37.8px) מקצות הדף. (היסטוריה: 0.2 מקורי → 0.5 → 1.0 הנוכחי.)
- **שמירה אוטומטית לא שמרה freeObjs** — `scheduleSave()` קורא כעת ל-`window.saveDoc` (wrapper) ולא לפונקציית-הבסיס.
- **גלגלת עכבר על טאבים** — wheel event על `.tabs` מעבר בין לשוניות.
- **פתיחה ריקה** — `init()` פותח מנהל מסמכים אם יש מסמכים שמורים אך הקישור האחרון לא עובד.
- **סרגל קובץ מורחב** — נוספו: "שמור בשם", "מאפיינים" (info dialog), "שמירה אוטומטית" toggle.

### באגים שתוקנו (סשנים קודמים)
- **חפיפת כותרת/תחתית** — `paginateBlocks()` שומר `1.8cm` בראש עמודי-המשך ו-`1.6cm` בתחתית כשיש כותרת/תחתית רצה (בלי כותרת — ללא שינוי).
- **פלטות צבע/גודל מוסתרות** — קיבלו `z-index: 10050` מעל הסרגל הצף (`#ctxBar` = 9998).
- **"עוד אפשרויות"** — נפתח תמיד כלפי מטה (עם גלילה אם צריך), לא מתהפך מעלה.
- **שינוי גודל גופן** — הבחירה נשמרת (בלוק יחיד: הכנסה דרך Range), `readCurrentFontSizePt()` קורא את הגודל האמיתי — כך אפשר ללחוץ ±​ ברצף והסרגל לא נעלם.
- **Ctrl+N** — חווט (קודם פתח חלון דפדפן חדש).
